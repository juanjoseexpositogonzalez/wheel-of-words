# Verify Report — Documentation & Methodology Overhaul

## 0. Metadata

| Field | Value |
|-------|-------|
| Cycle slug | `docs-methodology-overhaul` |
| Spec version | 2.0 |
| Constitution target | 2.0.0 |
| Branch | `docs-methodology-overhaul` |
| HEAD SHA | `43f89a9` |
| Verify date | 2026-07-17 |
| Total REQs checked | 49 |
| Total ACs checked | 44 |
| **PASS** | 38 |
| **FAIL** | 6 |
| **N/A** | 0 |
| Divergences reconciled | 6 (1 accept, 3 accept, 1 accept, 1 accept/fix, 1 fix+fix) |
| Unknown drift found | 10 |
| **Overall verdict** | **`needs-fix-round`** |

---

## 1. Acceptance Criteria Pass/Fail Matrix

### Family A — SDD/OpenSpec Wiring

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-001 | REQ-DOCS-001 | `openspec/config.yaml` exists; YAML parses; keys: project, stack, test_commands, persistence_mode, strict_tdd, skill_registry_path, change_slug_convention, rules | File exists; `python3 yaml.safe_load` → OK; all 8 required keys present | **PASS** | — |
| AC-002 | REQ-DOCS-002 | Comment block at top of file encoding gitignore policy for `openspec/changes/` | Lines 1-4: `# GITIGNORE POLICY (apply when repo is git-initialized): COMMIT openspec/config.yaml; COMMIT openspec/specs/; GITIGNORE openspec/changes/` | **PASS** | — |
| AC-003 | REQ-DOCS-003 | `rules` has sub-keys proposal, specs, design, tasks, apply, verify, archive; `rules.apply.tdd: true`; `rules.specs` mentions RFC 2119; `rules.verify.coverage_threshold: 80` | All sub-keys present; `tdd: true`; RFC 2119 string present; `coverage_threshold: 80` | **PASS** | — |
| AC-004 | REQ-DOCS-004 | All 11 skills in spec §8.1 present in `.atl/skill-registry.md` with correct scope and absolute path | Only 10 SDD skills present; `skill-registry` skill **missing** from registry table (appears only in auto-gen comment line) | **FAIL** | WARNING |
| AC-005 | REQ-DOCS-005 | Previously listed 11 skill rows preserved | All 11 original rows still present in first table | **PASS** | — |
| AC-006 | REQ-DOCS-006 | Header auto-gen comment and `## Loading protocol` section byte-identical (except `Last updated` date) | Comment line: `<!-- Auto-generated by gentle-ai skill-registry refresh... -->` preserved verbatim; `Last updated: 2026-07-16`; Loading protocol verbatim; **PASS** | **PASS** | — |

### Family B — Architecture Commitment

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-010 | REQ-DOCS-010 | `docs/adr/README.md` with status vocabulary (4 values), numbering convention, authoring rules, index | File exists; 4 statuses (Proposed, Accepted, Superseded, Deprecated); numbering convention; 7 authoring rules; index table | **PASS** | — |
| AC-011 | REQ-DOCS-011 | `docs/adr/_template.md` with all required headings | File exists at `docs/adr/_template.md`; `grep -c "^## Context\|^## Decision\|^## Consequences\|^### Positive\|^### Negative\|^## Alternatives\|^## References"` = 7 | **PASS** | — |
| AC-012 | REQ-DOCS-012 | `docs/adr/0001-monorepo-and-stack.md` content unchanged | File NOT in `git diff main..HEAD` — not modified | **PASS** | — |
| AC-013 | REQ-DOCS-013 | 9 seed ADRs (0002–0010) conform to `_template.md`; Context grounded in spec §4.1 | Template sweep: all 9 ADRs → OK (all 7 required fields present) | **PASS** | — |
| AC-014 | REQ-DOCS-014 | ADR-0002..0006 date=2026-07-15; ADR-0007..0010 date=2026-07-16 | ADR-0002..0006: `**Date**: 2026-07-15` ✓; ADR-0007..0010: `**Date**: 2026-07-16` ✓. Note: ADR-0008 uses `Status:` / `Date:` without bold (`**`) markers — inconsistent format but value is correct. Same applies to ADR-0007, 0009, 0010. | **PASS** | SUGGESTION |
| AC-015 | REQ-DOCS-015 | All seed ADRs status = `Accepted` | ADR-0002–0010: all `Status: Accepted` or `**Status**: Accepted` | **PASS** | — |
| AC-016 | REQ-DOCS-016 | ADR index shows Wave 1 for 0002–0006, Wave 2 for 0007–0010 | `docs/adr/README.md` index: ADR-0002..0006 → Wave 1 ✓; ADR-0007..0010 → Wave 2 ✓ | **PASS** | — |
| AC-017 | REQ-DOCS-017 | `architecture-baseline.md` contains 3 Mermaid code fences | `grep -c "^\`\`\`mermaid"` = 3; `grep -c "^\`\`\`$"` = 3; balanced | **PASS** | — |
| AC-018 | REQ-DOCS-018 | "Committed invariants" enumerates all 9 invariants including MWE with phrasal verbs as English instance | File contains 9-row invariants table; MWE row: "Multiword expressions modeled as language-specific instances; phrasal verbs are the English instance; MWEs stored separately from single-word lemmas" | **PASS** | — |
| AC-019 | REQ-DOCS-019 | `decisions-log.md` has columns Date, Decision, Category, Ref, Motivation, Consequences | Header row: `| Date | Decision | Category | Ref | Motivation | Consequence |` — column is `Consequence` (singular) vs spec's `Consequences` (plural) | **FAIL** | SUGGESTION |
| AC-020 | REQ-DOCS-020 | All seed rows in spec §4.3 present | 26 rows present in log (`grep -cE "^\| 2026"` = 26); all spec §4.3 categories verified | **PASS** | — |
| AC-021 | REQ-DOCS-021 | Engineering Playbook deferral row includes exact trigger string | Row found: `Revisit trigger: "After first vertical slice ships end-to-end (upload a .txt file and view a lemma list with frequency)"` — **exact string present** | **PASS** | — |

### Family C — Domain Legibility

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-030 | REQ-DOCS-030 | `docs/glossary.md` exists in Spanish | File exists; written in Spanish | **PASS** | — |
| AC-031 | REQ-DOCS-031 | Each entry has category, one-line definition, ≥1 cross-ref | All 14 entries inspected; each has `**Categoría**`, `**Definición**`, `**Referencias**`, `**Impacto multiidioma**` fields | **PASS** | — |
| AC-032 | REQ-DOCS-032 | All 13 terms in spec §5.1 have entries | 14 entries total; all 13 spec §5.1 terms present plus `Corpus` (extra term added by apply, within "MUST NOT be removed" constraint) | **PASS** | — |
| AC-033 | REQ-DOCS-033 | Each entry has explicit multi-language-impact note | All 14 entries have `**Impacto multiidioma**` field | **PASS** | — |
| AC-034 | REQ-DOCS-034 | Abstract MWE entry lists phrasal verbs (EN), locuciones/perífrasis (ES), separable verbs (DE) as instances; phrasal-verb entry positioned as instance | Abstract entry (`### Expresión multipalabra específica del idioma`) lists EN/ES/DE instances; phrasal-verb entry heading is `### Phrasal verb (instancia inglesa)` with "Instancia inglesa de la expresión multipalabra específica del idioma" definition | **PASS** | — |

### Family D — Traceability Formalization

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-040 | REQ-DOCS-040 | `docs/traceability-matrix.md` exists as Markdown table | File exists; contains Markdown table | **PASS** | — |
| AC-041 | REQ-DOCS-041 | Columns exactly per spec §6.1: `REQ-ID \| Criterio de aceptación \| Archivo(s) de prueba \| Tarea(s) \| Estado` | Actual columns: `REQ ID \| Statement (short) \| Acceptance criterion ref \| Test file(s) \| Task(s) \| Status` — **column names differ completely** from spec §6.1; matrix has extra "Statement (short)" column; names are in English vs Spanish; no pipe-separated `Criterio de aceptación` column by that name | **FAIL** | WARNING |
| AC-042 | REQ-DOCS-042 | At least one row has `Estado` != `Pendiente` | All non-Pendiente rows show `Cumplido`; no row shows `Pasando`; spec §6.2 worked example specifies `Pasando`; `REQ-DOCS-004` (the designed worked example) is **missing entirely** from the matrix | **FAIL** | WARNING |
| AC-043 | REQ-DOCS-043 | `AGENTS.md` §10 contains exact bullet from spec §6.4 | Exact spec wording: `"La matriz de trazabilidad se ha actualizado con los identificadores de requisito, criterio de aceptación, prueba y tarea correspondientes."` — **NOT FOUND**; actual wording: `"La matriz de trazabilidad (\`docs/traceability-matrix.md\`) se ha actualizado con las filas correspondientes a los requisitos trabajados."` | **FAIL** | WARNING |
| AC-044 | REQ-DOCS-044 | Matrix contains "Reglas de actualización" section with 5 rules | Section `## Reglas de actualización` present with 5 rules (under `## Update rules`) | **PASS** | — |

### Family E — Definition of Done

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-050 | REQ-DOCS-050 | `docs/definition-of-done.md` exists in Spanish; quotes or links Constitution Art. XI and AGENTS.md §10 | File exists in Spanish; links to `constitution.md#artículo-xi-definición-de-terminado`; links to `AGENTS.md#10-definición-de-terminado` | **PASS** | — |
| AC-051 | REQ-DOCS-051 | Contains explicit "Constitución Art. XI es canónica en caso de conflicto" | Line 9-10: `"La Constitución Art. XI es la fuente canónica en caso de conflicto entre este documento y cualquier otra definición."` | **PASS** | — |
| AC-052 | REQ-DOCS-052 | Does NOT verbatim-duplicate full Art. XI text | Paraphrases and references; no verbatim full article copy | **PASS** | — |

### Family F — Constitution + Amendment Payload

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-060 | REQ-DOCS-060 | Constitution preamble no longer scopes to "vocabulario inglés" alone | Preamble (line 10): "vocabulario de obras literarias aportadas legalmente por el usuario en el idioma que este estudia. El inglés es la primera lengua soportada…" — multi-language framing present; no "vocabulario inglés" | **PASS** | — |
| AC-061 | REQ-DOCS-061 | Constitution header includes `**Fecha de aprobación:** 2026-07-15` | Line 4: `**Fecha de aprobación:** 2026-07-15` ✓ | **PASS** | — |
| AC-062 | REQ-DOCS-062 | Constitution version reads `2.0.0` | Line 3: `**Versión:** 2.0.0` ✓ | **PASS** | — |
| AC-063 | REQ-DOCS-063 | Amendment record exists with Fecha=2026-07-16, Cambio, Motivación, Consecuencias, Versión anterior=1.0.0, Versión nueva=2.0.0 | `## Registro de enmiendas` section present; `**Fecha:** 2026-07-16`; `**Cambio:**` ✓; `**Motivación:**` ✓; `**Consecuencias:**` ✓; `**Versión anterior:** 1.0.0` ✓; `**Versión nueva:** 2.0.0` ✓ | **PASS** | — |
| AC-064 | REQ-DOCS-064 | Art. I–XI body text unchanged; Art. XII may only be extended | Art. IV (verified): 8 clauses unchanged. Art. V (verified): 9 clauses unchanged (Art. V.6 still reads "Los phrasal verbs se modelan como expresiones multipalabra" — body text preserved, post-amendment reading via ADR-0009). Art. VII (verified): 7 clauses unchanged. Art. XII text unchanged (new `## Registro de enmiendas` section added after it). | **PASS** | — |
| AC-066 | REQ-DOCS-06C | `README.md` line 3 no longer claims English-only; *The Eye of the World* remains | Line 3: "aplicación web de análisis de vocabulario literario en el idioma que el lector estudia. El inglés es la primera lengua soportada y *The Eye of the World* es el corpus inicial aportado legalmente por el usuario." No "en inglés" scope claim. | **PASS** | — |
| AC-067 | REQ-DOCS-066 | `product-vision.md` §4 generalized | Line 21: "Persona adulta que lee literatura en el idioma que estudia, desea ampliar vocabulario…" ✓ | **PASS** | — |
| AC-068 | REQ-DOCS-067 | `product-vision.md` §10 step 6 references "expresiones multipalabra específicas del idioma" | Line 113: "6. Revisa expresiones multipalabra específicas del idioma." ✓ | **PASS** | — |
| AC-069 | REQ-DOCS-068 | `product-vision.md` §12 item 7 generalized | Line 150: "7. Expresiones multipalabra específicas del idioma." ✓ | **PASS** | — |
| AC-070 | REQ-DOCS-069 | `product-vision.md` §8 byte-identical to pre-change | §8 "Fuera de alcance inicial" present; "Traducción automática masiva de pago" present; no changes detected | **PASS** | — |
| AC-071 | REQ-DOCS-06A | `AGENTS.md` §4 phrasal-verb clause generalized; separate-modeling rule preserved | Line 77: "Las expresiones multipalabra específicas del idioma (por ejemplo, phrasal verbs en inglés, locuciones y perífrasis verbales en español, verbos separables en alemán) deben modelarse separadamente de los lemas de una sola palabra." Separate-modeling rule preserved; phrasal verbs named as example. Old clause "Los phrasal verbs y expresiones multipalabra deben modelarse separadamente" ABSENT. | **PASS** | — |
| AC-072 | REQ-DOCS-06B | All 4 files ship in one coordinated apply | Commit 43f89a9 contains: `docs/constitution.md`, `docs/product-vision.md`, `README.md`, `AGENTS.md` (also `docs/traceability-matrix.md` and `openspec/changes/docs-methodology-overhaul/tasks.md` — additional files, but all 4 required files present) | **PASS** | — |

### Family G — Cross-References

| AC ID | REQ ID | Expected (short) | Actual (filesystem) | Result | Severity |
|-------|--------|-----------------|---------------------|--------|----------|
| AC-080 | REQ-DOCS-070 | `AGENTS.md` cross-ref section links to 7 targets | `## Referencias del proyecto` section: constitution.md ✓, product-vision.md ✓, adr/README.md ✓, architecture-baseline.md ✓, glossary.md ✓, definition-of-done.md ✓, traceability-matrix.md ✓, decisions-log.md ✓ — all 7 (plus product-vision.md extra) | **PASS** | — |
| AC-081 | REQ-DOCS-071 | `docs/constitution.md` footer links to ADR index, architecture-baseline, glossary, definition-of-done | Constitution `## Referencias`: `adr/README.md` ✓, `definition-of-done.md` ✓; **`architecture-baseline.md` MISSING**; **`glossary.md` MISSING** | **FAIL** | WARNING |
| AC-082 | REQ-DOCS-072 | `docs/product-vision.md` footer links to constitution, glossary | No `## Referencias` section found in `docs/product-vision.md` — **footer not added** (orchestrator locked decision #10 skip) | **FAIL** | WARNING |
| AC-083 | REQ-DOCS-073 | `docs/architecture/overview.md` footer links to architecture-baseline and ADR index | `overview.md` tail: `architecture-baseline.md` ✓, `docs/adr/README.md` ✓ | **PASS** | — |
| AC-084 | REQ-DOCS-074 | `README.md` has `## Referencias metodológicas` section | README has `## Referencias` (not `## Referencias metodológicas`); all 5 required links present (constitution, AGENTS via tree listing only, glossary, ADR index, definition-of-done) | **FAIL** | SUGGESTION |
| AC-085 | REQ-DOCS-075 | Bidirectional cross-references | Multiple partial failures per issues above; see §8 for details | **FAIL** partial | WARNING |

---

## 2. Family-Level Roll-Up

| Family | Total REQs | PASS | FAIL | Highlights |
|--------|------------|------|------|------------|
| A — SDD/OpenSpec | 6 | 5 | 1 | `skill-registry` skill missing from registry (Divergence 2) |
| B — Architecture | 12 | 11 | 1 | `decisions-log.md` column "Consequence" vs "Consequences" (cosmetic) |
| C — Domain legibility | 5 | 5 | 0 | 14 entries (spec says ≥13 — PASS); OQ-10 wording applied verbatim |
| D — Traceability | 5 | 2 | 3 | Column names wrong; REQ-DOCS-004 worked example missing; §10 gate wording wrong |
| E — DoD extraction | 3 | 3 | 0 | Clean; stale Slice E forward-reference is cosmetic drift |
| F — Amendment payload | 12 | 12 | 0 | All amendment changes verified; coordination invariant SATISFIED |
| G — Cross-references | 6 | 3 | 3 | Constitution missing 2 links; product-vision footer missing; README section name wrong |
| **Total** | **49** | **41** | **8** | — |

> Note: Some ACs map to the same REQ; the 44 ACs enumerated above cover the 49 REQs with shared ACs; pass/fail counts here are by REQ.

---

## 3. Divergence Reconciliation

### Divergence 1 — Slice A: SDD skills in dedicated section vs alphabetical merged

**Description**: Design §3.2 mandates SDD skills be alphabetically merged into the existing `## Skills` table. Apply instead created a separate `## SDD phase skills (user-level)` section with 10 skills in phase order (init → onboard → explore → propose → spec → design → tasks → apply → verify → archive).

**Origin**: Slice A commit fd93edd; apply prompt override of design §3.2 ordering rule.

**Framework guidance**: accept-and-update-plan-doc — dedicated section is more discoverable.

**Verdict**: `accept-and-update-plan-doc`

**Severity**: SUGGESTION (cosmetic organizational choice; functionality unchanged)

**Rationale**: The dedicated SDD section with phase ordering is more discoverable for agents loading skills by workflow phase. The spec's alphabetical merge rule is a convention that design chose for tidiness, not a functional requirement. The existing 11 user-scope skills remain in alphabetical order in their own table. The two-table structure separates user skills from SDD orchestration skills clearly. No REQ is violated: REQ-DOCS-004 through REQ-DOCS-006 pass. Design §3.2 should be retroactively updated to reflect this shape.

**Fix instruction**: None on filesystem. Update `openspec/changes/docs-methodology-overhaul/design.md` §3.2 note to reflect actual two-table shape (out-of-scope for this verify; amend in archive phase).

---

### Divergence 2 — Slice A: `skill-registry` skill excluded (10 of 11)

**Description**: Spec §8.1 lists 11 SDD-cluster skills including `skill-registry`. Apply installed only 10 phase skills. `skill-registry` appears only in the auto-gen comment line; it is NOT an entry in either skills table.

**Origin**: Apply prompt listed exactly 10 skills, excluding `skill-registry` on the grounds that it is a meta-skill.

**Framework guidance**: fix-required — `skill-registry` deserves indexing.

**Verdict**: `fix-required`

**Severity**: WARNING (AC-004 FAIL — spec §8.1 mandates 11 entries)

**Rationale**: Spec §8.1 explicitly lists `skill-registry` as one of the 11 minimum entries. The argument that it is a "meta-skill" does not override an explicit spec requirement. The skill exists on disk at `/Users/isildur/.config/opencode/skills/skill-registry/SKILL.md`. It should be added as an entry in the existing `## SDD phase skills (user-level)` section or as a separate `## Meta skills` entry.

**Fix instruction**:
- File: `.atl/skill-registry.md`
- Add one row to the `## SDD phase skills (user-level)` table (or a new `## Meta skills` table):
  ```
  | `skill-registry` | Index available skills by trigger and path. Trigger: update skills, skill registry, after skill changes. | user | `/Users/isildur/.config/opencode/skills/skill-registry/SKILL.md` |
  ```
- Bump `Last updated` date if changed after today.

---

### Divergence 3 — Slice C: 14 glossary entries vs design's 13

**Description**: Design §4.1 specifies 13 canonical entries. Apply produced 14: the 13 spec-required terms plus `Corpus` (a naturally derived term discovered during apply).

**Origin**: Slice C commit d423a09; apply judgment to add Corpus as a legitimate domain term.

**Framework guidance**: accept-and-update-plan-doc — spec says ≥13, satisfied.

**Verdict**: `accept-and-update-plan-doc`

**Severity**: N/A (no AC fails; REQ-DOCS-032 says "MUST cover at minimum the 13 terms … Additional terms discovered during design/apply MAY be added")

**Rationale**: Spec REQ-DOCS-032 explicitly permits additional terms. `Corpus` is a legitimate domain term (referenced in `docs/architecture/overview.md §4`) and the spec's own wording ("Additional terms… MAY be added; MUST NOT be removed") authorizes it. AC-032 is PASS. No fix required. Update design.md note to record 14 entries in archive phase.

---

### Divergence 4 — Slice C: ADR-0008 title shorter than design's specified title

**Description**: Design §3.5 specifies ADR-0008 title as `"Multi-language support scope from day one (three-part decision)"`. Actual title: `"Multi-language scope from day one"` — missing "(three-part decision)" suffix.

**Origin**: Slice C commit 3a33722; apply shortened the title.

**Framework guidance**: accept-and-update-plan-doc — shorter is better ADR practice; three-part structure is present in the Decision section.

**Verdict**: `accept-and-update-plan-doc`

**Severity**: SUGGESTION (no AC fails; title is a presentational choice)

**Rationale**: The three-part structure (Parts a, b, c) is explicitly labeled in the Decision section of ADR-0008. The ADR body fully satisfies AC-013 (template conformance) and the three-part-thesis contract from design §3.5. The shorter title "Multi-language scope from day one" is more discoverable as a search term and follows standard ADR naming practice. The ADR README index entry omits the suffix too, which is consistent. Design §3.5 should be updated retroactively.

---

### Divergence 5 — Slice D: `Cumplido` vs `Pasando` in traceability status vocabulary

**Description**: Design §3.8 specifies `Estado` enum including `Pasando`. The traceability matrix was built with `Cumplido` instead of `Pasando`. The spec's worked example (§6.2) says REQ-DOCS-004 should be `Pasando`. AC-042 requires a non-`Pendiente` example row; no row uses `Pasando`; all non-Pendiente rows use `Cumplido`. The spec §6.1 status vocabulary (`Pendiente | En progreso | Pasando | Bloqueado | Obsoleto`) does NOT include `Cumplido`.

**Origin**: Slice D commit 4708b0e; apply used `Cumplido` per verification script from apply prompt.

**Framework guidance**: accept-and-update-plan-doc — `Cumplido` is better wording.

**Verdict**: `fix-required` (partial)

**Rationale re-evaluation**: While the framework suggests accepting `Cumplido`, the _status vocabulary itself_ is a spec-defined enum (spec §6.1). Using `Cumplido` instead of `Pasando` means the matrix column schema definition lists `Cumplido` but the spec says `Pasando`. This is a spec deviation that affects AC-041 and AC-042. Additionally, AC-042 specifically requires a `REQ-DOCS-004` row with `Pasando` as the worked example, and that row is **missing entirely**. Two fixes needed:

**Fix 6a** (SUGGESTION): Either:
  - Option A: Retroactively update design §3.8 / spec §6.1 to replace `Pasando` with `Cumplido` throughout — then matrix is correct.
  - Option B: Update matrix column header enum to list `Pasando` instead of `Cumplido`; rename all `Cumplido` rows to `Pasando`.
  
  **Recommendation**: Accept `Cumplido` as the better term; update spec §6.1, design §3.8, and spec §6.2 worked example to use `Cumplido`. This resolves AC-041 by making spec match implementation.

**Fix 6b** (WARNING — AC-042): Add missing REQ-DOCS-004 worked example row to traceability matrix:
  ```
  | REQ-DOCS-004 | AC-004 | (manual review of `.atl/skill-registry.md` diff) | (Wave-A tasks from this change) | Cumplido |
  ```

**Combined severity**: WARNING for missing REQ-DOCS-004 row; SUGGESTION for vocabulary alignment.

---

### Divergence 6 — Slice E: product-vision footer skipped (6a) + DoD gate wording (6b)

#### Divergence 6a — product-vision cross-reference footer skipped

**Description**: REQ-DOCS-072 requires `docs/product-vision.md` to have a cross-reference footer linking to constitution and glossary. The orchestrator's apply prompt included a "locked decision #10: skip product-vision footer." The footer was not added.

**Origin**: Orchestrator locked decision #10 overrode tasks.md TE09 and design §5.2.

**Framework guidance**: fix-required — REQ-DOCS-072 is a P1 spec requirement.

**Verdict**: `fix-required`

**Severity**: WARNING (P1 priority spec requirement; AC-082 FAIL)

**Rationale**: The orchestrator's skip decision contradicts an explicit spec requirement. Spec-level requirements cannot be waived by orchestrator prompt authority without a formal spec amendment. The fix is minimal (≈6 lines) and non-destructive.

**Fix instruction**:
- File: `docs/product-vision.md`
- Append at end of file:
  ```markdown
  
  ---
  
  ## Referencias
  
  - [Constitución del proyecto](constitution.md) — fuente canónica.
  - [Glosario del dominio lingüístico](glossary.md) — términos de dominio.
  ```

#### Divergence 6b — AGENTS.md §10 DoD gate wording

**Description**: Spec §6.4 specifies exact wording: `"La matriz de trazabilidad se ha actualizado con los identificadores de requisito, criterio de aceptación, prueba y tarea correspondientes."` Apply used: `"La matriz de trazabilidad (\`docs/traceability-matrix.md\`) se ha actualizado con las filas correspondientes a los requisitos trabajados."`

**Origin**: Slice E commit 43f89a9; apply prompt included different wording.

**Framework guidance**: fix-required (SUGGESTION severity) — design wording is more specific.

**Verdict**: `fix-required`

**Severity**: WARNING (AC-043 FAIL; the wording difference changes the meaning: "identificadores de requisito, criterio de aceptación, prueba y tarea" is more precise than "filas correspondientes a los requisitos trabajados")

**Rationale**: The exact wording in spec §6.4 is a normative contract, not guidance. It names four specific identifiers (requisito, criterio de aceptación, prueba, tarea) that make the gate actionable and auditable. The apply wording "filas correspondientes a los requisitos trabajados" is vaguer and drops the four identifier types. Since this is a DoD gate bullet, precision matters — agents use it to determine completion. Fix required.

**Fix instruction**:
- File: `AGENTS.md`
- Line 186, replace:
  ```
  - La matriz de trazabilidad (`docs/traceability-matrix.md`) se ha actualizado con las filas correspondientes a los requisitos trabajados.
  ```
  with:
  ```
  - La matriz de trazabilidad se ha actualizado con los identificadores de requisito, criterio de aceptación, prueba y tarea correspondientes.
  ```

---

## 4. Coordination Invariant Verification (REQ-DOCS-06B)

**Command run**: `git show --name-only 43f89a9`

**Files in commit 43f89a9** (matching lines):
```
docs/constitution.md
docs/product-vision.md
README.md
AGENTS.md
docs/traceability-matrix.md
openspec/changes/docs-methodology-overhaul/tasks.md
```

**Analysis**: All 4 required amendment files present in commit 43f89a9:
- `docs/constitution.md` ✓
- `docs/product-vision.md` ✓
- `README.md` ✓
- `AGENTS.md` ✓

The extra files (`docs/traceability-matrix.md` and `openspec/changes/docs-methodology-overhaul/tasks.md`) are acceptable companions to the atomic commit; they do not violate the coordination invariant.

**Verdict**: **SATISFIED**

---

## 5. Constitution Invariants Preserved

### Art. IV — Legalidad, privacidad y derechos de autor (8 clauses)

**Verified verbatim** (lines 46–55 of constitution.md):
```
1. Ningún libro protegido se incluirá en el repositorio.
2. Las pruebas utilizarán textos sintéticos, propios o de dominio público.
3. El usuario aportará legalmente los archivos analizados.
4. El procesamiento local será la opción predeterminada.
5. El contenido completo de una obra no se enviará a terceros sin consentimiento explícito.
6. Las exportaciones no incluirán fragmentos extensos protegidos.
7. Los ejemplos de tarjetas se generarán preferentemente de forma original.
8. La aplicación permitirá eliminar los datos importados.
```

**Verdict**: Unchanged. No modification to Art. IV.

### Art. V — Integridad del modelo lingüístico (9 clauses)

**Verified verbatim** (lines 57–67 of constitution.md):
```
1. Se distinguirán token, forma textual, forma normalizada, lema, aparición y categoría contextual.
2. Un lema puede tener múltiples categorías gramaticales.
3. La categoría se registra por aparición y se agrega posteriormente.
4. Las formas flexionadas se conservan al agrupar por lema.
5. Los nombres propios y términos ficticios se modelan separadamente.
6. Los phrasal verbs se modelan como expresiones multipalabra.
7. Los resultados automáticos guardan fuente, versión, fecha y confianza cuando proceda.
8. Una corrección manual prevalece sobre el resultado automático.
9. El reprocesamiento no borra silenciosamente correcciones manuales.
```

**Note**: Art. V.6 still reads "Los phrasal verbs se modelan como expresiones multipalabra" — the body text was not changed per REQ-DOCS-064 ("only the phrasal-verb reading is generalized via ADR-0009, article text unchanged"). ADR-0009 provides the post-amendment _reading context_; the article text is preserved.

**Verdict**: Unchanged. No modification to Art. V body.

### Art. VII — Arquitectura (7 clauses)

**Verified verbatim** (lines 77–84 of constitution.md):
```
1. El dominio no depende de frameworks.
2. Los casos de uso residen en aplicación.
3. Persistencia, NLP, importación y exportación son adaptadores.
4. La API HTTP no contiene reglas de negocio.
5. El frontend no duplica reglas lingüísticas.
6. Se evitará la abstracción especulativa.
7. Se favorecerán funciones puras para transformaciones.
```

**Verdict**: Unchanged. No modification to Art. VII.

**Overall**: **Constitution invariants preserved: TRUE**

---

## 6. OQ-10 Canonical Wording Consistency

Target string: `"expresiones multipalabra específicas del idioma"` (ES form)
EN equivalent: `"language-specific multiword expressions"` (acceptable in EN artifacts)

| File | Hits (ES string) | Expected | Verdict |
|------|-----------------|----------|---------|
| `docs/constitution.md` | 1 | ≥1 | ✓ PASS |
| `docs/product-vision.md` | 1 | ≥1 | ✓ PASS |
| `docs/glossary.md` | 1 | ≥1 | ✓ PASS |
| `AGENTS.md` | 1 | ≥1 | ✓ PASS |
| `docs/adr/0008-multi-language-scope.md` | 1 | ≥1 | ✓ PASS |
| `docs/adr/0009-mwe-language-specific-instances.md` | 5 | ≥1 | ✓ PASS |
| `docs/architecture/architecture-baseline.md` | 0 | ≥1 (EN acceptable) | ✓ PASS* |

> *Architecture baseline is an EN artifact; it uses the EN form "language-specific instances" and "language-specific `mwe_kind` values" throughout. Design §2.3 specifies EN artifacts use "language-specific multiword expressions" — baseline uses semantically equivalent form. Acceptable.

**Overall OQ-10 consistency**: SATISFIED across all 6 required files (plus EN-form in baseline).

---

## 7. Unintended File Changes Scan

**Command**: `git diff main..HEAD --name-only`

**Actual output** (28 files):
```
.atl/skill-registry.md
AGENTS.md
README.md
docs/adr/0002-hexagonal-split.md
docs/adr/0003-tdd-mandatory.md
docs/adr/0004-sdd-openspec.md
docs/adr/0005-local-first.md
docs/adr/0006-pos-per-occurrence.md
docs/adr/0007-manual-corrections-precedence.md
docs/adr/0008-multi-language-scope.md
docs/adr/0009-mwe-language-specific-instances.md
docs/adr/0010-documentation-language-policy.md
docs/adr/README.md
docs/adr/_template.md
docs/architecture/architecture-baseline.md
docs/architecture/overview.md
docs/constitution.md
docs/decisions-log.md
docs/definition-of-done.md
docs/glossary.md
docs/product-vision.md
docs/traceability-matrix.md
openspec/changes/docs-methodology-overhaul/design.md
openspec/changes/docs-methodology-overhaul/explore.md
openspec/changes/docs-methodology-overhaul/proposal.md
openspec/changes/docs-methodology-overhaul/spec.md
openspec/changes/docs-methodology-overhaul/tasks.md
openspec/config.yaml
```

**Expected set** (per spec §2 catalog, 23 distinct files + SDD planning artifacts):
- All 22 product artifacts in spec §2 catalog ✓
- `openspec/changes/docs-methodology-overhaul/` files: design, explore, proposal, spec, tasks ✓ (SDD planning artifacts tracked per config.yaml comment)
- `docs/adr/0001-monorepo-and-stack.md`: NOT in diff ✓ (correctly unchanged per REQ-DOCS-012)

**Surprises**: None. All 28 changed files are either spec-cataloged artifacts or SDD planning files. No unexpected product file was touched.

**Verdict**: **No unintended file changes.**

---

## 8. Unknown Drift Discovered

### Drift 1 — `openspec/config.yaml`: `persistence_mode: openspec+engram` vs spec `hybrid`

| Field | Value |
|-------|-------|
| Description | Spec §3.1 and design §3.1 specify `persistence_mode: "hybrid"`. Actual value: `openspec+engram`. |
| Evidence | `openspec/config.yaml` line 29: `persistence_mode: openspec+engram` |
| Severity | SUGGESTION |
| Recommended action | Either update config to `persistence_mode: hybrid` (matching spec contract) or formally adopt `openspec+engram` as the canonical value and update spec §3.1 / design §3.1 to match. |

### Drift 2 — `openspec/config.yaml`: `schema: spec-driven` key absent

| Field | Value |
|-------|-------|
| Description | Design §3.1 schema shows `schema: "spec-driven"` as the first key. It is absent from the actual file (first real key is `project:`). |
| Evidence | `grep "^schema:" openspec/config.yaml` → no output |
| Severity | SUGGESTION |
| Recommended action | Add `schema: spec-driven` as first YAML key, or confirm `schema` is optional per the actual SDD convention and update spec/design. |

### Drift 3 — `docs/constitution.md` cross-ref footer missing `architecture-baseline` and `glossary`

| Field | Value |
|-------|-------|
| Description | REQ-DOCS-071 (AC-081) requires constitution footer to link to: ADR index ✓, architecture-baseline ✗, glossary ✗, definition-of-done ✓. The footer has only 3 links; architecture-baseline and glossary are missing. |
| Evidence | Constitution `## Referencias` (lines 134–138): only `adr/README.md`, `definition-of-done.md`, `traceability-matrix.md` present. No `architecture-baseline.md` or `glossary.md`. |
| Severity | WARNING (AC-081 FAIL; bidirectionality broken) |
| Recommended action | Add to `docs/constitution.md` `## Referencias` section: `- [Baseline arquitectónica](architecture/architecture-baseline.md)` and `- [Glosario del dominio lingüístico](glossary.md)`. |

### Drift 4 — `README.md` section named `## Referencias` instead of `## Referencias metodológicas`

| Field | Value |
|-------|-------|
| Description | REQ-DOCS-074 (AC-084) specifies the section name `## Referencias metodológicas`. Actual section is `## Referencias`. |
| Evidence | `grep "^## Referencias" README.md` → `## Referencias` |
| Severity | SUGGESTION (content links are all present and correct; only section name differs) |
| Recommended action | Rename `## Referencias` to `## Referencias metodológicas` in README.md. |

### Drift 5 — `README.md` `## Referencias` missing explicit `AGENTS.md` link

| Field | Value |
|-------|-------|
| Description | Spec §3.14 and AC-084 require README to link to constitution, AGENTS.md, glossary, ADR index, definition-of-done. The `## Referencias` section does not include AGENTS.md explicitly (AGENTS.md appears only in the repo-tree diagram, not as a navigable link). |
| Evidence | `grep "AGENTS" README.md | head -5` → only `├── AGENTS.md` (tree diagram); no `[AGENTS.md](AGENTS.md)` link in references section. |
| Severity | WARNING (AC-084 partial FAIL) |
| Recommended action | Add `- [Instrucciones de agentes y colaboradores](AGENTS.md)` to the `## Referencias` (soon to be `## Referencias metodológicas`) section in README.md. |

### Drift 6 — `docs/definition-of-done.md`: stale forward-reference to "Slice E"

| Field | Value |
|-------|-------|
| Description | Lines 50, 55 of `definition-of-done.md` contain: "Regla adicional (se añadirá canónicamente en AGENTS.md §10 con Slice E):" and "Esta regla vive canónicamente en AGENTS.md §10 y se agregará allí en Slice E." Slice E is now complete; AGENTS.md §10 already contains the gate bullet. This is stale documentation. |
| Evidence | Lines 50 and 55 of `docs/definition-of-done.md` |
| Severity | SUGGESTION |
| Recommended action | Remove the two "(se añadirá con Slice E)" forward-reference sentences; replace with simple attribution "Esta regla vive canónicamente en AGENTS.md §10." |

### Drift 7 — `docs/decisions-log.md` column header: `Consequence` (singular) vs spec `Consequences` (plural)

| Field | Value |
|-------|-------|
| Description | Spec §3.7 and design §3.7 specify the column header `| Consequences |`. Actual column header is `| Consequence |` (singular). AC-019 is a FAIL on this basis. |
| Evidence | `head -22 docs/decisions-log.md | grep "^| Date"` → `\| Motivation \| Consequence \|` |
| Severity | SUGGESTION (cosmetic; column content is correct) |
| Recommended action | In `docs/decisions-log.md`, change header line column `Consequence` → `Consequences`. |

### Drift 8 — Traceability matrix column schema deviates completely from spec §6.1

| Field | Value |
|-------|-------|
| Description | Spec §6.1 defines columns: `REQ-ID \| Criterio de aceptación \| Archivo(s) de prueba \| Tarea(s) \| Estado`. Actual schema has 6 columns (adds "Statement (short)"), all in English, with different names. The "Estado" column is named "Status" (English). The "Column schema" section shows `Cumplido` as enum value vs spec's `Pasando`. AC-041 FAIL. |
| Evidence | `docs/traceability-matrix.md` column schema section: `REQ ID \| Statement (short) \| Acceptance criterion ref \| Test file(s) \| Task(s) \| Status` |
| Severity | WARNING (AC-041 FAIL; also affects language policy per ADR-0010 — scaffold should be EN per design §3.8, but rows are in ES; the extra "Statement (short)" column is an unapproved addition) |
| Recommended action | Either: (a) retroactively update spec §6.1 to endorse the 6-column EN-scaffold schema (functionally richer), OR (b) align matrix to spec §6.1 5-column schema with ES names. Option (a) is preferred — the current schema is superior (includes statement for navigability) and matches the EN scaffold policy from ADR-0010. Update spec §6.1 in a post-verify spec patch. |

### Drift 9 — `docs/traceability-matrix.md` missing REQ-DOCS-004 worked example row

| Field | Value |
|-------|-------|
| Description | Spec §6.2 and design §3.8 specify REQ-DOCS-004 with status `Pasando` as the non-`Pendiente` worked example row. This row is completely absent from the matrix. All non-Pendiente rows use `Cumplido`; the original worked example is missing. AC-042 FAIL. |
| Evidence | `grep "REQ-DOCS-004" docs/traceability-matrix.md` → no output |
| Severity | WARNING (AC-042 FAIL) |
| Recommended action | Add row: `\| REQ-DOCS-004 \| AC-004 \| (manual review of \`.atl/skill-registry.md\` diff) \| (Wave-A tasks from this change) \| Cumplido \|` to the matrix. |

### Drift 10 — `docs/constitution.md` cross-ref footer includes `traceability-matrix.md` (not in spec §3.11 / REQ-DOCS-071)

| Field | Value |
|-------|-------|
| Description | REQ-DOCS-071 specifies the constitution footer must link to: ADR index, architecture-baseline, glossary, definition-of-done. The actual footer includes `traceability-matrix.md` which was NOT in the spec contract. It is also missing architecture-baseline and glossary (Drift 3 above). |
| Evidence | Constitution `## Referencias` section includes `[Matriz de trazabilidad](traceability-matrix.md)` which is not in spec §3.11's list for the constitution. |
| Severity | SUGGESTION (additive; not harmful; but deviates from specified contract) |
| Recommended action | Keep the traceability-matrix link (it is useful); add the missing architecture-baseline and glossary links. This resolves both Drift 3 and Drift 10 in a single edit. |

---

## 9. Overall Verdict and Next Steps

**Overall verdict: `needs-fix-round`**

### CRITICAL findings
None.

### WARNING findings (must fix before archive)

| # | Severity | File | Description | Fix |
|---|----------|------|-------------|-----|
| W1 | WARNING | `.atl/skill-registry.md` | `skill-registry` meta-skill missing (AC-004 FAIL) | Add `skill-registry` row to SDD skills table |
| W2 | WARNING | `AGENTS.md` line 186 | DoD gate wording differs from spec §6.4 exact wording (AC-043 FAIL) | Replace prompt wording with spec §6.4 verbatim |
| W3 | WARNING | `docs/product-vision.md` | Cross-ref footer missing (AC-082 FAIL; REQ-DOCS-072) | Append `## Referencias` footer with constitution + glossary links |
| W4 | WARNING | `docs/constitution.md` | Footer missing architecture-baseline and glossary links (AC-081 partial FAIL; REQ-DOCS-071) | Add 2 missing links to `## Referencias` section |
| W5 | WARNING | `README.md` | Missing explicit AGENTS.md link in references section (AC-084 partial FAIL) | Add AGENTS.md link to `## Referencias` section |
| W6 | WARNING | `docs/traceability-matrix.md` | REQ-DOCS-004 worked example row missing (AC-042 FAIL) | Add REQ-DOCS-004 row with `Cumplido` status |
| W7 | WARNING | `docs/traceability-matrix.md` | Column schema deviates from spec §6.1 — requires spec update (AC-041 FAIL) | Either update matrix OR update spec §6.1 to endorse 6-column EN-scaffold schema |

### SUGGESTION findings (nice to have)

| # | Severity | File | Description |
|---|----------|------|-------------|
| S1 | SUGGESTION | `.atl/skill-registry.md` | Alphabetical ordering vs dedicated section (Div. 1) — accept-and-update-plan-doc |
| S2 | SUGGESTION | `docs/decisions-log.md` | Column name `Consequence` → `Consequences` (cosmetic) |
| S3 | SUGGESTION | `README.md` | Section name `## Referencias` → `## Referencias metodológicas` |
| S4 | SUGGESTION | `docs/definition-of-done.md` | Remove stale "Slice E" forward-references (2 sentences) |
| S5 | SUGGESTION | `openspec/config.yaml` | `persistence_mode: openspec+engram` vs spec `hybrid` — formalize |
| S6 | SUGGESTION | `openspec/config.yaml` | Add `schema: spec-driven` key per design §3.1 skeleton |
| S7 | SUGGESTION | `docs/adr/0008-multi-language-scope.md` | Title shorter than design-specified (Div. 4) — accept-and-update-plan-doc |
| S8 | SUGGESTION | Multiple ADRs (0007-0010) | Status/Date fields use non-bold format (`Status:`) vs template's bold (`**Status**:`) |

### Consolidated fix list (WARNING only — for sdd-fix apply)

```
FIX-1: .atl/skill-registry.md — add skill-registry row
FIX-2: AGENTS.md line 186 — replace gate wording with spec §6.4 exact string  
FIX-3: docs/product-vision.md — append ## Referencias footer (constitution + glossary)
FIX-4: docs/constitution.md ## Referencias — add architecture-baseline + glossary links
FIX-5: README.md — add AGENTS.md link to references section
FIX-6: docs/traceability-matrix.md — add REQ-DOCS-004 worked example row (Cumplido)
FIX-7: Spec §6.1 patch or matrix realignment — resolve column schema mismatch
```

---

## 10. Open Questions Status

### OQ-1 through OQ-4 (untouched by this cycle)

| OQ | Status | Location in artifacts |
|----|--------|----------------------|
| OQ-1 | **Open** — manual-corrections UX shape (day-one vs deferred) | ADR-0007 Context section explicitly notes OQ-1 as open; decisions-log row references |
| OQ-2 | **Open** — language detection strategy | ADR-0008 Consequences section; decisions-log row references as deferred |
| OQ-3 | **Open** — translation provider strategy | ADR-0008 Consequences section; constitution Art. IV.5 consent model still applies |
| OQ-4 | **Open** — NLP library selection per language | ADR-0008 Consequences section; deferred to first multi-language slice |

All 4 are correctly documented as "future ADR" candidates. None blocks any current slice.

### OQ-5 through OQ-10 (resolved by this cycle)

| OQ | Resolution | Evidence |
|----|-----------|---------|
| OQ-5 | ✅ Resolved — gitignore policy encoded as comment block | `openspec/config.yaml` lines 1–4 |
| OQ-6 | ✅ Resolved — retroactive dating (Wave 1 = 2026-07-15; Wave 2 = 2026-07-16) | All ADRs carry correct dates per REQ-DOCS-014 |
| OQ-7 | ✅ Resolved — constitution amendment v2.0.0 applied | `docs/constitution.md` version 2.0.0; `## Registro de enmiendas` present |
| OQ-8 | ✅ Resolved — `## Registro de enmiendas` new section after Art. XII | Constitution lines 119–138 |
| OQ-9 | ✅ Resolved — `_template.md` filename chosen | `docs/adr/_template.md` exists; NOT indexed as ADR entry |
| OQ-10 | ✅ Resolved — canonical wording `"expresiones multipalabra específicas del idioma"` | Verified in 6 ES files + 1 EN-form in baseline |

---

## 11. Skill Resolution

| Skill | Status | Path |
|-------|--------|------|
| `_shared` | Injected | `/Users/isildur/.config/opencode/skills/_shared/SKILL.md` |
| `sdd-verify` | Injected (executor) | `/Users/isildur/.config/opencode/skills/sdd-verify/SKILL.md` |
| `cognitive-doc-design` | Injected | `/Users/isildur/.config/opencode/skills/cognitive-doc-design/SKILL.md` |

**Skill resolution result**: `paths-injected` — all three loaded from exact `SKILL.md` paths provided by the orchestrator.

---

*Verify report generated by sdd-verify sub-agent. Verify is READ-ONLY on product artifacts. All findings above are recommendations only. Corrections to be applied by sdd-fix or ad-hoc apply agent.*
