# Traceability Matrix — wheel-of-words

## Purpose

This file provides a cross-spec view of requirements, acceptance criteria, test
files, tasks, and status across the entire repository. It complements the
per-spec `traceability.md` files (which remain the source of truth for
feature-scoped rows). This top-level matrix helps agents and reviewers navigate
the full requirement landscape without opening every spec directory.

---

## Column schema

| Column | Type | Description |
|--------|------|-------------|
| REQ ID | string | `REQ-<feature>-<n>` identifier — matches the format in each spec |
| Statement (short) | string | One-line summary of the requirement |
| Acceptance criterion ref | string | AC-ID reference or path to spec acceptance section |
| Test file(s) | string | Path(s) to test file(s) or test-plan IDs |
| Task(s) | string | T-ID reference(s) from the corresponding tasks file |
| Status | enum | `Pendiente` · `En progreso` · `Cumplido` · `Bloqueado` |

---

## Matrix

| REQ ID | Statement (short) | Acceptance criterion ref | Test file(s) | Task(s) | Status |
|--------|--------------------|--------------------------|--------------|---------|--------|
| REQ-005-001 | Las unidades de estudio se agrupan por el par `(lemma, POS)` efectivo, con recuentos por aparición y orden estable | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-01 | `apps/api/tests/unit/test_vocabulary_repository_properties.py::test_the_returned_sequence_is_ordered_by_count_desc_then_lemma_then_pos_with_null_first`, `apps/api/tests/integration/test_vocabulary_repository.py::test_homograph_is_returned_as_two_pos_groups_with_occurrence_counts`, `::test_groups_follow_literal_design_order_and_are_stable_between_reads`, `apps/api/tests/api/test_vocabulary_route.py::test_get_vocabulary_returns_the_versioned_ordered_group_contract`, `::test_identical_vocabulary_requests_preserve_the_entire_body_order` | T8, T22, T35 | Cumplido |
| REQ-005-002 | La agrupación usa los valores efectivos y respeta la precedencia por campo de las correcciones manuales | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-02 | `apps/api/tests/unit/test_vocabulary_repository_properties.py::test_property_the_repositorys_effective_resolution_agrees_with_resolve_effective`, `::test_property_v3_group_counts_equal_a_naive_python_groupby_over_resolve_effective`, `apps/api/tests/integration/test_vocabulary_repository.py::test_corrections_move_groups_vacate_raw_group_and_resolve_each_field` | T6–T8, T22 | Cumplido |
| REQ-005-003 | Los valores `NULL` de lema o POS permanecen como buckets visibles y etiquetados | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-03 | `apps/api/tests/integration/test_vocabulary_repository.py::test_unannotated_and_missing_pos_occurrences_remain_visible_buckets`, `apps/web/tests/components/VocabularyBrowser.test.tsx` | T22, T55–T56 | Cumplido |
| REQ-005-004 | El navegador expone una ruta propia y conserva el contrato de anotación sin cambios | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-04 | `apps/api/tests/api/test_vocabulary_route.py::test_vocabulary_is_additive_to_the_frozen_annotation_contract` | T35–T41 | Cumplido |
| REQ-005-005 | Un import desconocido devuelve `IMPORT_NOT_FOUND`; un import vacío devuelve un conjunto vacío correcto | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-05 | `apps/api/tests/integration/test_vocabulary_repository.py::test_unknown_book_and_existing_empty_book_are_distinct_states`, `apps/api/tests/api/test_vocabulary_route.py::test_unknown_vocabulary_import_returns_a_content_free_not_found_error`, `::test_deleted_vocabulary_import_returns_import_not_found` | T8, T22, T35, T67 | Cumplido |
| REQ-005-006 | El filtro POS reduce el conjunto de grupos, admite el bucket `NULL` y rechaza selectores inválidos | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-06 | `apps/api/tests/api/test_vocabulary_route.py::test_vocabulary_pos_selector_narrows_groups_without_changing_their_counts`; `::test_vocabulary_pos_selector_can_select_the_null_pos_bucket`; `::test_vocabulary_rejects_an_invalid_pos_selector`; `::test_vocabulary_pos_selector_without_matching_groups_is_an_empty_success`; `apps/web/tests/pages/ImportPage.test.tsx` | T63–T66 | Cumplido |
| REQ-005-007 | Ningún comportamiento del navegador usa confianza ni expone agregados o parámetros de confianza | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-07 | `apps/api/tests/unit/test_vocabulary_repository_isolation.py::test_the_vocabulary_repository_never_references_annotation_provenance`, `apps/api/tests/unit/test_no_confidence_action_or_propn_filter.py::test_nothing_acts_on_confidence_anywhere_in_the_package`, `::test_a_min_confidence_query_parameter_would_be_caught`, `::test_a_mean_confidence_property_would_be_caught`, `::test_a_sort_by_confidence_helper_would_be_caught` | T12, T18–T21 | Cumplido |
| REQ-005-008 | La capacidad puede leer correcciones para resolver precedencia, pero no escribe filas `manual_correction` ni ofrece una corrección en la UI | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-08 | `apps/api/tests/unit/test_vocabulary_write_guard.py::test_no_module_writes_to_manual_correction`, `::test_writes_appended_to_the_vocabulary_repository_are_caught`, `::test_select_and_attribute_reads_are_permitted`, `apps/web/tests/components/VocabularyBrowser.test.tsx` | T12–T17, T55–T56 | Cumplido |
| REQ-005-009 | Los grupos se calculan en cada consulta; no se persiste un agregado y el índice aditivo revierte | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-09 | `apps/api/tests/integration/test_alembic_0004.py::test_upgrade_adds_the_group_index_downgrade_removes_it`, `::test_downgrade_touches_no_other_schema_object`, `apps/api/tests/integration/test_vocabulary_repository.py::test_corrections_move_groups_vacate_raw_group_and_resolve_each_field`, `apps/api/tests/api/test_vocabulary_route.py::test_vocabulary_read_refreshes_after_a_correction_committed_between_requests` | T1–T5, T22, T68 | Cumplido |
| REQ-005-010 | El frontend presenta la respuesta recibida sin derivar reglas lingüísticas y conserva un único mapa UPOS | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-10 | `apps/web/tests/components/VocabularyBrowser.test.tsx`, `apps/web/tests/components/uposLabels.test.ts`, `apps/web/tests/contracts/no-linguistic-rules.test.ts`, `apps/web/tests/contracts/no-lemma-naming.test.ts` | T47–T49, T52–T57 | Cumplido |
| REQ-005-011 | La respuesta sin paginación se mantiene dentro de los presupuestos documentados y se mide con un benchmark ejecutable | `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md` — AC-005-11 | `apps/api/tests/integration/test_vocabulary_bench.py::test_response_body_budget_is_derived_from_the_external_import_size_limit`, `::test_vocabulary_response_stays_within_the_named_budget_at_the_occurrence_ceiling`, `::test_lowering_each_named_bound_below_its_measurement_fails`; `apps/api/tests/api/test_vocabulary_route.py` | T42–T46, T69–T71 | Cumplido |

**Design obligation note — vocabulary-browser D1a / WU2b.** Snapshot isolation across the two
repository query legs is not a `REQ-005-*` row because `openspec/changes/vocabulary-browser/specs/005-vocabulary-browser/spec.md`
§7 excludes transaction boundaries and spec §5 `AMB-10` records that the obligation lives in design
D1. It is tracked through `openspec/changes/vocabulary-browser/design.md` D1a and tasks T72–T76, with
coverage in `apps/api/tests/integration/test_vocabulary_repository_snapshot.py` and
`apps/api/tests/integration/test_engine.py::test_create_engine_from_url_uses_wal_for_local_sqlite_files`.
| REQ-003H-001 | Las exenciones de los guards de nombres se vinculan al nombre y a su sitio propietario; el helper es único | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-01 | `apps/api/tests/unit/test__guard_binding.py`, `apps/api/tests/unit/test_no_lemma_naming.py`, `apps/api/tests/unit/test_annotation_contract.py`, `apps/web/tests/contracts/no-lemma-naming.test.ts` (B6: conjuntos propietarios por archivo y control de borde M3) | 1.1–1.10 (`08a-guard-binding-helper`); 2.1–2.4 (`08b-frontend-owning-sets`) | Cumplido |
| REQ-003H-002 | La exención de docstring del guard de aislamiento se limita al módulo revisado y fija su contenido exacto | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-02 | `apps/api/tests/unit/test_annotation_write_repository_isolation.py` (mutación de docstring de módulo, fijación E4, controles M2/M3) | 3.1–3.7 (`08c-docstring-instance-exemption`) | Cumplido |
| REQ-003H-003 | Las afirmaciones sobre internals del modelo se generan en tiempo de ejecución o se omiten de la prosa | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-03 | `apps/api/tests/unit/test_no_model_internal_claims.py`, `apps/api/tests/integration/test_attribute_ruler_enumeration.py::test_runtime_enumeration_has_only_computed_shape_and_partition_predicates` | 4.1–4.11 (`08d-model-claim-enumeration`) | Cumplido |
| REQ-003H-004 | El puerto del analizador documenta cada obligación que la aplicación valida | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-04 | `apps/api/tests/unit/test_annotation_ports.py::test_analyze_docstring_states_the_source_index_failure_obligation`, `::test_each_annotation_validation_rejection_has_a_port_obligation`, `apps/api/tests/unit/test_annotate_import.py::test_same_text_swap_with_consistently_reassigned_source_index_is_accepted` | 5.1–5.3, 5.8 (`08e-port-contract-bound`) | Cumplido |
| REQ-003H-005 | La matriz describe el mecanismo enviado, sin afirmar equivalencia de objetos para el emparejamiento | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-05 | `apps/api/tests/unit/test_traceability.py::test_every_cited_python_test_node_resolves_against_the_collected_suite`, `::test_cited_test_resolution_reports_a_nonexistent_node`, `::test_cited_test_resolution_expands_a_relative_node_reference`, `::test_matrix_contains_no_identity_based_pairing_claim` | 6.1–6.7 (`08f-traceability-matrix`) | Cumplido |
| REQ-003H-006 | Garantía acotada de `source_index`: the check proves that the analyzer's output is **self-consistent with the input it was given** — each annotation reports both the token text and the input index it claims to have been computed for, and both MUST agree with the occurrence at that position, so an internally reordered result of equal length is rejected instead of being written to the wrong occurrence. It does **not** prove that the annotation is linguistically correct for that token, and it cannot detect an analyzer that swaps two same-text annotations while consistently reassigning `source_index`, because `source_index` is self-reported by the analyzer. | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003H-06 | `apps/api/tests/unit/test_annotation_ports.py::test_bounded_source_index_guarantee_is_verbatim_in_all_required_locations`, `apps/api/tests/unit/test_annotate_import.py::test_same_text_swap_with_consistently_reassigned_source_index_is_accepted`, `::test_same_text_swap_without_reassigning_source_index_fails_and_writes_nothing` | 5.1–5.8 (`08e-port-contract-bound`) | Cumplido |
| REQ-001-001 | Backend FastAPI arrancable mediante un comando documentado | `specs/001-project-foundation/acceptance.md#AC-001` | `apps/api/tests/api/test_health.py` | TA10, TB110–TB111 | Cumplido |
| REQ-001-002 | `GET /api/v1/health` devuelve el contrato documentado | `specs/001-project-foundation/acceptance.md#AC-001` | `apps/api/tests/api/test_health.py` | TB107–TB111 | Cumplido |
| REQ-001-003 | Frontend React + TypeScript arrancable mediante un comando documentado | `specs/001-project-foundation/acceptance.md#AC-002` | `apps/web/e2e/status.spec.ts` | TA06–TA10, TC11 | Cumplido |
| REQ-001-004 | Frontend muestra carga, disponible, no disponible y reintento | `specs/001-project-foundation/acceptance.md#AC-003,AC-004` | `apps/web/tests/components/StatusPage.test.tsx` | TC03–TC11 | Cumplido |
| REQ-001-005 | Backend conecta SQLite mediante URL configurable | `specs/001-project-foundation/acceptance.md#AC-005` | `apps/api/tests/integration/test_engine.py` | TB201–TB202 | Cumplido |
| REQ-001-006 | Alembic configurado con migración inicial verificable | `specs/001-project-foundation/acceptance.md#AC-006` | `apps/api/tests/integration/test_alembic.py` | TB205–TB207 | Cumplido |
| REQ-001-007 | Configuración por entorno con valores de ejemplo seguros | `specs/001-project-foundation/acceptance.md#AC-007` | `apps/api/tests/unit/test_settings.py` | TA11, TB101–TB102 | Cumplido |
| REQ-001-008 | Superficie de comandos unificada para instalar, desarrollar, probar y migrar | `specs/001-project-foundation/acceptance.md#AC-008` | `apps/api/tests/unit/test_traceability.py` | TA10, TD06 | Cumplido |
| REQ-001-009 | Suite con pruebas backend unitarias, API y SQLite | `specs/001-project-foundation/acceptance.md#AC-009` | `apps/api/tests/{unit,api,integration}/` | TB101–TB112, TB201–TB208 | Cumplido |
| REQ-001-010 | Pruebas frontend cubren carga, disponible, no disponible y reintento | `specs/001-project-foundation/acceptance.md#AC-010` | `apps/web/tests/components/StatusPage.test.tsx` | TC03–TC10 | Cumplido |
| REQ-001-011 | Playwright valida el estado integrado de salud | `specs/001-project-foundation/acceptance.md#AC-011` | `apps/web/e2e/status.spec.ts` | TD01–TD02 | Cumplido |
| REQ-001-012 | Backend pasa Ruff y mypy con configuración acordada | `specs/001-project-foundation/acceptance.md#AC-012` | `.github/workflows/ci.yml` | TA02, TD03, TD09 | Cumplido |
| REQ-001-013 | Frontend pasa TypeScript estricto y ESLint | `specs/001-project-foundation/acceptance.md#AC-012` | `.github/workflows/ci.yml` | TA06–TA08, TD03, TD09 | Cumplido |
| REQ-001-014 | GitHub Actions ejecuta instalación, calidad, pruebas, E2E y migraciones | `specs/001-project-foundation/acceptance.md#AC-012` | `apps/api/tests/unit/test_ci_workflow.py` | TD03, TD09 | Cumplido |
| REQ-001-015 | Capas backend hexagonales con fronteras de framework | `docs/adr/0002-hexagonal-split.md#decision` | inspección estructural de `apps/api/src/wheel_vocabulary/{domain,application,infrastructure,api}/` | TA04, TD04–TD05 | Cumplido |
| REQ-001-016 | README explica requisitos, instalación, arranque, pruebas, calidad, migraciones y estructura | `specs/001-project-foundation/acceptance.md#AC-015` | `apps/api/tests/unit/test_traceability.py` | TD06 | Cumplido |
| REQ-001-017 | El repositorio no incluye texto de libros protegido | `specs/001-project-foundation/acceptance.md#AC-013` | inspección de contenido versionado | TA12, TD09 | Cumplido |
| REQ-001-018 | Entornos, bases locales, importaciones, cobertura, cachés y secretos están ignorados | `specs/001-project-foundation/acceptance.md#AC-007` | `.gitignore` | TA12 | Cumplido |
| REQ-CI-001 | Backend CI jobs install locked development tooling independently | `openspec/archive/2026-08-10-fix-pr5-ci-tooling/specs/001-project-foundation/spec.md#AC-CI-001` | `apps/api/tests/unit/test_ci_workflow.py` | TC01, TC03, TC05–TC06 | Cumplido |
| REQ-CI-002 | Frontend CI uses the repository-root lockfile and frozen installs | `openspec/archive/2026-08-10-fix-pr5-ci-tooling/specs/001-project-foundation/spec.md#AC-CI-002` | `apps/api/tests/unit/test_ci_workflow.py` | TC02, TC04–TC06 | Cumplido |
| REQ-CI-003 | E2E CI prepares locked backend and frontend dependencies | `openspec/archive/2026-08-10-fix-pr5-ci-tooling/specs/001-project-foundation/spec.md#AC-CI-003` | `apps/api/tests/unit/test_ci_workflow.py` | TC01, TC03, TC05–TC06 | Cumplido |
| REQ-CI-004 | CI repair does not mask unrelated functional E2E failures | `openspec/archive/2026-08-10-fix-pr5-ci-tooling/specs/001-project-foundation/spec.md#AC-CI-004` | `apps/api/tests/unit/test_ci_workflow.py`, `apps/web/e2e/status.spec.ts` | TC02, TC04, TC06 | Cumplido |
| REQ-DOCS-004 | Skill registry lists all SDD phase skills | `openspec/changes/docs-methodology-overhaul/spec.md#AC-004` | N/A — inspección | TA02 | Cumplido |
| REQ-DOCS-010 | `docs/adr/README.md` con índice, vocabulario de estado, convención de numeración y reglas de autoría | `openspec/changes/docs-methodology-overhaul/spec.md#AC-010` | N/A — inspección | TB01 | Cumplido |
| REQ-DOCS-030 | `docs/glossary.md` en español con todos los términos canónicos del dominio lingüístico (≥ 13 entradas) | `openspec/changes/docs-methodology-overhaul/spec.md#AC-030` | N/A — inspección | TC07, TC08 | Cumplido |
| REQ-DOCS-060 | Preámbulo de la constitución generalizado: eliminar "inglés" como único ámbito; framing multi-idioma | `openspec/changes/docs-methodology-overhaul/spec.md#AC-060` | N/A — inspección | TE02 | Cumplido |
| REQ-DOCS-062 | Constitución bumpeada a v2.0.0 (MAJOR) con sección de registro de enmiendas | `openspec/changes/docs-methodology-overhaul/spec.md#AC-062` | N/A — inspección | TE01, TE03 | Cumplido |
| REQ-DOCS-06C | `README.md` línea 3 generalizada: eliminar "en inglés" como calificador de ámbito | `openspec/changes/docs-methodology-overhaul/spec.md#AC-066` | N/A — inspección | TE08 | Cumplido |
| REQ-DOCS-066 | `docs/product-vision.md` §4 usuario generalizado: "en el idioma que estudia" | `openspec/changes/docs-methodology-overhaul/spec.md#AC-067` | N/A — inspección | TE04 | Cumplido |
| REQ-DOCS-06A | `AGENTS.md` §4 cláusula MWE generalizada con wording OQ-10 canónico | `openspec/changes/docs-methodology-overhaul/spec.md#AC-071` | N/A — inspección | TE09 | Cumplido |
| REQ-DOCS-06B | Invariante de coordinación: los cuatro archivos de enmienda aterrizan atómicamente en un único commit | `openspec/changes/docs-methodology-overhaul/spec.md#AC-072` | N/A — git log | TE01..TE13 | Cumplido |
| REQ-DOCS-043 | `AGENTS.md` §10 puerta DoD de trazabilidad añadida | `openspec/changes/docs-methodology-overhaul/spec.md#AC-043` | N/A — inspección | TE10 | Cumplido |
| REQ-TESTHYG-001 | Las pruebas de integración liberan cada motor SQLAlchemy que abren; la suite no emite `ResourceWarning` | issue #14 — criterios de aceptación | `apps/api/tests/integration/conftest.py`, `apps/api/tests/integration/{test_alembic,test_base,test_engine}.py`, filtro en `apps/api/pyproject.toml` | TH01–TH03 | Cumplido |
| REQ-TESTHYG-002 | El cliente HTTP de pruebas usa el transporte soportado por `starlette.testclient` (`httpx2`); cero `StarletteDeprecationWarning` | issue #15 — criterios de aceptación | `apps/api/tests/api/test_health.py`, `apps/api/tests/api/test_health_security.py` | TH04 | Cumplido |
| REQ-TESTHYG-003 | El filtro `filterwarnings` convierte en error ambas clases de aviso para que no puedan reaparecer en silencio | issues #14 y #15 — criterios de aceptación | `apps/api/pyproject.toml` (`[tool.pytest.ini_options]`) | TH05 | Cumplido |
| REQ-002-005 | Tokenización y `normalize()` puras y genéricas de idioma, sin frameworks ni literales ISO-639 en `domain/` | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-06, AC-002-07 | `apps/api/tests/unit/test_tokenizer.py`, `apps/api/tests/unit/test_normalizer.py`, `apps/api/tests/unit/test_domain_isolation.py` | T1A01–T1A03, T1A04–T1A06, T1A10 | Cumplido |
| REQ-002-015 | `normalize` es idempotente: `normalize(normalize(x))` equivale a `normalize(x)` | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-20 | `apps/api/tests/unit/test_normalizer.py::test_normalize_is_idempotent` | T1A04–T1A06 | Cumplido |
| REQ-002-016 | El conjunto de formas normalizadas, sus frecuencias y sus formas de visualización no dependen del orden de entrada | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-21 | `apps/api/tests/unit/test_frequency.py::test_aggregation_is_order_independent_hypothesis` | T1A07–T1A09 | Cumplido |
| REQ-002-017 | Las frecuencias nunca son negativas: toda fila listada tiene un entero `>= 1` | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-22 | `apps/api/tests/unit/test_frequency.py::test_frequencies_are_never_negative_hypothesis` | T1A07–T1A09 | Cumplido |
| REQ-002-006 | Agregación por forma normalizada con frecuencia y orden (§2.4) | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-08, AC-002-09 | `apps/api/tests/unit/test_frequency.py::test_repeated_forms_collapse_with_frequency_and_sum`, `apps/api/tests/api/test_imports.py::test_rows_arrive_already_ordered_by_the_grouping_key`, `apps/api/tests/api/test_imports.py::test_get_imports_returns_the_ordered_table_with_the_persisted_id`, `::test_get_imports_diacritic_insensitive_order` | T1A07–T1A09 (leg de dominio); T1B13–T1B16 (leg de respuesta `POST`); T211–T212 (leg de lectura `GET`) | Cumplido — cerrado en el corte 2: la ruta `GET /api/v1/imports/{id}` reutiliza `domain.frequency.build_table()`, la misma implementación que la ruta de importación (spec §1.2, requisito transversal) |
| REQ-002-018 | Cada grupo muestra una forma de superficie real, elegida de forma determinista (D1–D3) | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-23, AC-002-24 | `apps/api/tests/unit/test_frequency.py::test_majority_and_tie_break_display_form`, `apps/api/tests/api/test_imports.py::test_each_row_carries_both_the_grouping_key_and_the_display_form`, `::test_get_imports_returns_the_ordered_table_with_the_persisted_id` | T1A07–T1A09 (leg de dominio); T1B15–T1B16 (`display_form` en la respuesta `POST`); T202 (sin columna nueva), T211–T212 (leg de lectura `GET`) | Cumplido — cerrado en el corte 2: `0002_book_occurrence` no añade columna para `display_form` (AC-002-24); se deriva por agregación sobre `Occurrence.raw_text`, ya persistido por REQ-002-010 |
| REQ-002-001 | La importación solo acepta subida multiparte de un `.txt`; se rechaza ruta de sistema de archivos, URL o texto en línea | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-01 | `apps/api/tests/api/test_imports.py`, `apps/api/tests/api/test_imports_cors.py` | T1B15–T1B17 | Cumplido |
| REQ-002-002 | Validación de extensión y tipo de contenido antes de decodificar o tokenizar un solo byte | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-02 | `apps/api/tests/unit/test_import_text.py`, `apps/api/tests/api/test_imports.py` | T1B10, T1B13–T1B14 | Cumplido |
| REQ-002-003 | Límite de tamaño configurable (`MAX_IMPORT_SIZE_BYTES`, 4 MiB por defecto) aplicado durante la lectura | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-03, AC-002-04 | `apps/api/tests/unit/test_settings.py`, `apps/api/tests/unit/test_import_text.py`, `apps/api/tests/api/test_imports.py` | T1B01–T1B02, T1B11, T1B13–T1B14 | Cumplido |
| REQ-002-004 | Decodificación UTF-8 estricta con rechazo accionable; BOM inicial tolerado y eliminado | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-05 | `apps/api/tests/unit/test_text_extraction.py`, `apps/api/tests/api/test_imports.py` | T1B06–T1B07 | Cumplido |
| REQ-002-012 | Un archivo vacío o solo con separadores importa correctamente con cero formas únicas | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-17 | `apps/api/tests/unit/test_import_text.py::test_a_content_free_upload_succeeds_with_zero_forms`, `apps/api/tests/api/test_imports.py::test_a_content_free_upload_is_a_success_with_a_zero_state` | T1B12–T1B14 | Cumplido |
| REQ-002-007 | Ni la clave de agrupación ni la forma mostrada se llaman «lemma» ni «lexeme» (MODIFICADO por `lemmatization-pos`: la lista de admisión enumerada sustituye la exigencia de cero coincidencias absolutas — ver REQ-003-023) | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-10; MODIFICADO por `openspec/specs/002-text-import/spec.md` — REQ-002-007/AC-002-10 | `apps/api/tests/unit/test_no_lemma_naming.py` (leg de backend + leg de columnas persistidas, `test_persisted_columns_contain_no_lemma_naming`; extendido en `lemmatization-pos` corte 1 con `_ALLOWED_LEMMA_SYMBOLS`, `_migration_modules`, `_reflected_column_names`; extendido en la remediación `judgment-day` C2 con `_schema_paths`/`_SCHEMA_OWNING_PATH_SEGMENTS`, glob de `api/schemas/*.json`); `apps/web/tests/contracts/no-lemma-naming.test.ts` (leg de frontend, incluye el describe `findViolations (remediation — pins the comment exemption directly)`; extendido con `ALLOWED_LEMMA_SYMBOLS`) | T1B20 (leg de backend); T1C14 (leg de frontend); T217 (leg de columnas persistidas); 1.6–1.8 (Phase 1, `lemmatization-pos`); C2 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido — cerrado en el corte 2 de `text-import` y modificado en el corte 1 de `lemmatization-pos`: el leg de columnas persistidas (T217) comprueba, con el mismo criterio AST, tanto `infrastructure/persistence/models.py` como toda migración bajo `migrations/versions/*.py`, más los nombres de columna reflejados desde todas las tablas de `Base.metadata` en tiempo de ejecución. Los tres legs están unificados en el mismo criterio estructural (AST para Python/TypeScript, JSON para el schema y el OpenAPI servido, metadata reflejada para las columnas persistidas). `lemmatization-pos` corte 1 añade una lista de admisión de nombres exactos (`_ALLOWED_LEMMA_SYMBOLS`/`ALLOWED_LEMMA_SYMBOLS`) para el lema genuino de REQ-003-006, sin excluir ningún archivo/directorio del escaneo (REQ-003-023). **C2**: antes de la corrección, el leg JSON de `test_no_lemma_naming.py` tenía fijada `_SCHEMA_PATH` a un único archivo (`import.v1.json`), pese a que AC-002-10 exige `api/schemas/*.json` — `health.v1.json` nunca se escaneaba y `annotation.v1.json` habría escapado igual de haber tenido una fuga. Ahora `_schema_paths()` recorre el directorio completo (`_SCHEMAS_DIR.glob("*.json")`), falla en cierre si el glob resuelve a cero archivos, y cada schema tiene su propio segmento de ruta propietario en `_SCHEMA_OWNING_PATH_SEGMENTS` (ninguno para `import.v1.json`/`health.v1.json`; `"occurrence"` — el segmento `$defs.occurrence`, el único componente que declara legítimamente `lemma`/`lemma_confidence` — para `annotation.v1.json`). Verificado en RED revirtiendo `_schema_paths()` a `[_SCHEMA_PATH]` y confirmando que las nuevas pruebas fallan exactamente como se esperaba; mutation-check adicional con una copia mutada de `health.v1.json` prueba que el escaneo alcanza ese archivo y detectaría una fuga real. **Corrección (R2, `judgment-day` ronda 2):** la corte C2 original vinculó `annotation.v1.json` a la cadena vacía `""` en vez de a un segmento propio — `"" in where` es `True` para cualquier ruta, así que era una exención de ARCHIVO COMPLETO disfrazada, sin ganancia neta de cobertura sobre no escanear el archivo en absoluto (verificado: renombrar la propiedad `raw_text` al nombre admitido `lemma` producía cero violaciones). `_json_violations` también comparaba la pertenencia con `owning_path_segment in where` — subcadena, no segmento exacto —, así que un componente OpenAPI futuro cuyo nombre solo CONTUVIERA `AnnotationOccurrenceResponse` como subcadena (p. ej. `AnnotationOccurrenceResponseExtra`) heredaba la exención sin poseerla (verificado con una clave `lemma` bajo ese componente sintético). Ambos se corrigieron: `_SCHEMA_OWNING_PATH_SEGMENTS["annotation.v1.json"]` ahora es `"occurrence"`, y `_json_violations` compara contra una lista de segmentos exactos (`_path_segments`, que separa por `.` y descarta el sufijo `` (key)``/`` (value)`` y los índices `[n]``) en vez de una subcadena cruda. Reproducido en RED con dos pruebas nuevas (`test_renaming_a_non_owning_property_of_annotation_schema_to_lemma_still_fails`, `test_a_sibling_openapi_schema_component_does_not_inherit_the_exemption`) |
| REQ-002-013 | Ningún registro de log contiene texto importado; los fallos se identifican por código e `import_id` | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-18 | `apps/api/tests/api/test_imports_logging.py` (leg de importación); `apps/api/tests/integration/test_book_repository.py::test_a_persistence_failure_during_create_logs_code_and_no_raw_text`, `::test_reading_an_unknown_import_logs_the_attempted_id` (leg de lectura y persistencia) | T1B18–T1B19 (leg de importación); T213–T214 (leg de lectura y persistencia) | Cumplido — cerrado en el corte 2: un fallo de persistencia durante `ImportText.create` se traduce a `PersistenceFailedError` (código `PERSISTENCE_FAILURE`, sin detalle de la excepción original) y una lectura `GET` con `id` desconocido registra `code=IMPORT_NOT_FOUND import_id=<id>` — nunca texto importado (spec §1.2, requisito transversal) |
| REQ-002-014 | El frontend no duplica ninguna regla lingüística: renderiza exactamente lo que devuelve la API, sin tokenizar, normalizar, ordenar ni derivar la forma mostrada | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-19 | `apps/web/tests/components/FrequencyTable.test.tsx::test_renders_received_order_and_display_form_verbatim`, `::test_frequency_column_is_not_colour_only`, `apps/web/tests/contracts/no-linguistic-rules.test.ts::test_import_modules_have_no_linguistic_rules`, `apps/web/tests/contracts/no-linguistic-rules.test.ts` describe block `findLinguisticRuleViolations` (pins comment exemption and detection of sort/toSorted/reverse/localeCompare/new Intl.Collator/Intl.Collator/toLowerCase/normalize/NFC) | T1C05–T1C09 | Cumplido |
| REQ-002-008 | Persistir `Book` + `Occurrence` (una fila por token emitido) vía repositorio y migración verificable | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-11, AC-002-12 | `apps/api/tests/integration/test_alembic_0002.py::test_upgrade_and_downgrade_book_occurrence`, `apps/api/tests/integration/test_book_repository.py::test_frequency_pairs_survives_a_new_session_against_the_same_database`, `::test_create_batches_occurrence_inserts_at_the_configured_size` | T201–T204, T207–T209 | Cumplido |
| REQ-002-009 | `content_hash` criptográfico (SHA-256, minúsculas, hex) en `Book`, calculado sobre los bytes crudos antes de decodificar | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-13 | `apps/api/tests/integration/test_book_repository.py::test_content_hash_matches_an_independently_computed_sha256`, `::test_a_one_byte_difference_changes_the_hash` | T206 | Cumplido |
| REQ-002-010 | `pos` reservado por aparición, siempre `None`, nunca global; `raw_text`/`normalized_text` permanecen columnas distintas | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-14 | `apps/api/tests/integration/test_occurrence_pos.py::test_every_persisted_occurrence_has_pos_none`, `::test_raw_text_and_normalized_text_stay_separate_values`, `::test_book_table_has_no_part_of_speech_column` | T205 | Cumplido |
| REQ-002-011 | Eliminar una importación y sus datos derivados, con confirmación explícita en la UI; el borrado es permanente, nunca lógico | `openspec/changes/text-import/specs/002-text-import/spec.md` — AC-002-15, AC-002-16 | `apps/api/tests/integration/test_delete_import.py`, `apps/api/tests/api/test_imports_cors.py::test_a_delete_preflight_is_allowed`, `apps/api/tests/unit/test_no_soft_delete.py`, `apps/web/tests/components/DeleteImportButton.test.tsx`, `apps/web/e2e/delete-import.spec.ts`; `apps/api/tests/integration/test_delete_orphans_annotation_data.py` (S1, `judgment-day` remediation) | T301–T310; S1 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido — corte 3, cierre de SPEC-002: `SqlAlchemyBookRepository.delete()` emite sentencias `DELETE` explícitas en una transacción, nunca `ON DELETE CASCADE` (design §6.2); la UI exige confirmación explícita antes de emitir la petición (Art. IX.5, rama de confirmación). **S1 (corrección `lemmatization-pos`)**: `delete()` solo borraba `occurrence` y `book`, nunca `manual_correction` ni `annotation_provenance` (las dos tablas hijas que añadió `lemmatization-pos`). Como `PRAGMA foreign_keys` está OFF (nunca se activa en `engine.py::create_engine_from_url`) el `ondelete="CASCADE"` declarado en el esquema no hace nada, y `Occurrence.id` es un ROWID de SQLite sin `sqlite_autoincrement`, reutilizable tras el borrado — reproducido de extremo a extremo antes de la corrección: al borrar el libro A y reimportar un libro B con el mismo recuento de ocurrencias, B reutiliza el id de ocurrencia liberado y el repositorio de lectura resuelve `pos_origin="manual"` con la corrección del libro A, nunca hecha sobre B. `delete()` ahora borra también `manual_correction`/`annotation_provenance` (vía subconsulta `IN (SELECT id FROM occurrence WHERE book_id = ...)`, no una lista Python — evita el mismo problema de límite de parámetros que C3 corrigió en otro lugar) antes de `occurrence`/`book`, en la misma transacción |
| REQ-003-001 | El runtime del backend está fijado a Python 3.12; el pin es prerrequisito duro antes de cualquier dependencia NLP | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-01 | `apps/api/tests/unit/test_python_pin.py` (los tres escenarios; el tercero vía inspección de `uv.lock`, corte de remediación) | 1.1–1.3 (Phase 1, `lemmatization-pos`); corte de remediación (`feat/spec-003-06-remediation`) | Cumplido — corte 1 de `lemmatization-pos` (capability `003-lemmatization-pos`): `pyproject.toml` declara `requires-python = ">=3.12,<3.14"`, `.python-version` fijado a `3.12` vía `uv python pin 3.12`, `[tool.mypy] python_version = "3.12"` coincide con el runtime resuelto (`3.12.13`). `spacy` y `en-core-web-sm` (dependencia URL-pinned, no `spacy download`) se instalan desde wheels precompiladas — cero compilación de C/Cython confirmada en el log de `uv add` (OQ-3, AC-003-01 escenario 3). **Corrección de hecho (`spec.md` §5 FACT-1):** la justificación original citaba `thinc 9.1.1` (solo cp312) como la restricción más estrecha; era falsa — `spacy 3.8.15` nunca resuelve thinc 9.x (exige `thinc<8.4.0,>=8.3.12`) y el `thinc 8.3.13` realmente resuelto publica wheels cp312/cp313/cp314. La restricción real es `spacy 3.8.15`, que solo publica cp312/cp313. Cota corregida a `<3.14`; `.python-version` permanece en `3.12` a propósito — `requires-python` expresa lo *soportado*, `.python-version` expresa lo *probado*. **Remediación (verify-report WARNING-5):** el escenario 3 («instala desde una wheel, no desde una build de código fuente») no era verificable desde dentro del intérprete en ejecución; el corte de remediación añade una prueba automatizada que inspecciona `uv.lock` (el artefacto estático que `uv sync` lee) y confirma que las entradas fijadas de `spacy`/`thinc` incluyen una wheel `cp312`, no solo un `sdist` |
| REQ-003-002 | El objeto de valor de anotación es puro y vive en el dominio; el guard de aislamiento del dominio se extiende a las bibliotecas NLP | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-02 | `apps/api/tests/unit/test_domain_isolation.py`; `apps/api/tests/unit/test_annotation_domain.py`; `apps/api/tests/unit/test_nlp_isolation.py` (leg de todo el paquete, corte de remediación) | 1.4–1.5 (Phase 1); 2.1–2.3 (Phase 2), `lemmatization-pos`; corte de remediación (`feat/spec-003-06-remediation`) | Cumplido — corte 2 cerró el objeto de valor (`domain/annotation.py::LinguisticAnnotation`, solo librería estándar, `_EXPECTED_MODULES` del guard de aislamiento nombrando `annotation.py` explícitamente). El corte de remediación cierra el segundo escenario de AC-003-02 («ningún tipo de spaCy escapa del adaptador», spec §8 hook H2), que había quedado sin guardar: `test_nlp_isolation.py` recorre TODO el paquete `wheel_vocabulary` (no solo `domain/`) y afirma que `spacy`/`thinc`/`stanza` se importan en exactamente un fichero, `infrastructure/nlp/spacy_analyzer.py`. Mutation-check real: se añadió temporalmente `import spacy` a `application/annotation/use_cases.py`, se observó el `AssertionError` nombrando el fichero y el import, se revirtió |
| REQ-003-003 | El puerto del analizador toma entrada pre-tokenizada y un idioma explícito, sin literal ISO-639 ni valor por defecto | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-03 | `apps/api/tests/unit/test_annotation_ports.py`; `apps/api/tests/unit/test_analyzer_registry.py`; `apps/api/tests/unit/test_settings.py::test_annotation_language_defaults_to_english`, `::test_analyzer_models_defaults_to_the_installed_english_pipeline`; `apps/api/tests/integration/test_spacy_analyzer.py::test_analyze_builds_the_doc_directly_and_never_calls_the_pipeline_as_text` | 2.7–2.8 (Phase 2); 4.1–4.8 (Phase 4), `lemmatization-pos` | Cumplido — corte 4 cierra las tres piezas pendientes: `infrastructure/nlp/spacy_analyzer.py::SpacyLinguisticAnalyzer` es el adaptador real, `infrastructure/nlp/registry.py::AnalyzerRegistry.resolve()` levanta `UnsupportedLanguageError` a partir de `analyzer_models` ANTES de invocar el loader (probado con un loader espía que registra cero llamadas para un idioma no configurado, nunca un fallback silencioso), y `Settings.annotation_language`/`Settings.analyzer_models` son el único lugar con un valor por defecto (`"en"`), fuera del puerto, del objeto de dominio y del esquema |
| REQ-003-005 | `pos` es una etiqueta UPOS registrada por aparición | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-05 | `apps/api/tests/unit/test_annotation_domain.py`, `apps/api/tests/unit/test_annotate_import.py`, `apps/api/tests/integration/test_spacy_analyzer.py`; `apps/api/tests/integration/test_attribute_ruler_enumeration.py::test_runtime_enumeration_has_only_computed_shape_and_partition_predicates` | Phase 4, `lemmatization-pos`; 4.1–4.4 (`08d-1-attribute-ruler-enumeration`) | Cumplido — el detalle del modelo se deriva en tiempo de ejecución por la enumeración citada, no se transcribe en esta matriz |
| REQ-003-006 | `lemma` es un tercer valor distinto, registrado por aparición, verbatim tal como lo emite el analizador | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-06 | `apps/api/tests/unit/test_annotation_domain.py::test_linguistic_annotation_fields_may_all_be_none`; `apps/api/tests/unit/test_annotation_models.py::test_occurrence_lemma_is_nullable_and_defaults_to_none`, `::test_occurrence_lemma_round_trips_a_stored_value`; `apps/api/tests/integration/test_annotation_write_repository.py::test_write_sets_pos_and_lemma_and_inserts_provenance`; `apps/api/tests/integration/test_spacy_analyzer.py::test_inflected_forms_share_one_lemma_while_keeping_distinct_forms`; `apps/api/tests/unit/test_annotate_import.py::test_a_whitespace_only_lemma_is_normalized_to_none_not_rejected` | 2.1–2.2 (Phase 2); 3.1–3.4, 3.8 (Phase 3); 4.2, 4.9–4.10 (Phase 4), `lemmatization-pos` | Cumplido — corte 4 cierra las dos piezas pendientes: el adaptador real puebla `lemma` verbatim desde `token.lemma` vía `vocab.strings` (L3, sin casefolding ni NFC), y `AnnotateImport` aplica la regla L4 — un valor de solo espacios en blanco persiste como `NULL`, nunca `""` — verificado con un analizador stub, la única vía por la que ese caso puede provocarse deliberadamente (el adaptador real de inglés nunca emite una cadena vacía para un token con letras) |
| REQ-003-008 | La confianza está acotada al intervalo válido o es `NULL`; ninguna capa fabrica, recorta ni redondea valores fuera de rango | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-08 | `apps/api/tests/unit/test_annotation_domain.py`, `apps/api/tests/integration/test_spacy_analyzer.py` | Phase 2, `lemmatization-pos`; S5 (`judgment-day` remediation) | Cumplido — el dominio valida el rango y el adaptador comprueba la normalización antes de publicar la confianza |
| REQ-003-010 | Una corrección manual gana en tiempo de lectura; el valor automático se conserva como auditoría y se expone un marcador de origen | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-10 | `apps/api/tests/unit/test_annotation_domain.py::test_resolve_effective_returns_automatic_when_no_correction_exists`, `::test_resolve_effective_returns_corrected_when_one_exists`, `::test_resolve_effective_does_not_mutate_or_discard_the_automatic_argument`, `::test_property_resolve_effective_output_is_never_a_third_value`; `apps/api/tests/integration/test_annotation_read_repository.py::test_a_seeded_correction_wins_on_read`, `::test_precedence_is_per_field_not_per_occurrence` | 2.4–2.6 (Phase 2); 3.9–3.10 (Phase 3), `lemmatization-pos` | Cumplido — la regla pura (corte 2) y su cableado no forjable (corte 3) están ambos cerrados: `domain/annotation.py::resolve_effective(automatic, corrected)` devuelve `(corrected, "manual")` cuando existe una corrección y `(automatic, "automatic")` en caso contrario, sin mutar ni descartar el valor automático (R1/R4/R5) — verificado con Hypothesis, el resultado es siempre exactamente uno de los dos valores de entrada, nunca un tercero (R1, C3). `infrastructure/persistence/annotation_repository.py::AnnotatedOccurrence` aplica la regla en `__post_init__` a partir de un `InitVar` de correcciones que se descarta tras construirse — el objeto resultante no tiene ningún campo que exponga un valor sin resolver, solo `effective_pos`/`pos_origin`/`automatic_pos` y `lemma`/`lemma_origin`/`automatic_lemma`. Verificado contra un `ManualCorrection` sembrado directamente (única vía en este corte, R6): la precedencia gana por campo, no por aparición |
| REQ-003-023 | El guard de nombres «lemma»/«lexeme» se estrecha a una lista de admisión enumerada y explícita, sin perder cobertura ni debilitarse a búsqueda textual | `openspec/specs/003-lemmatization-pos/spec.md` §P6 (AC-003-24); `openspec/specs/002-text-import/spec.md` — REQ-002-007/AC-002-10 | `apps/api/tests/unit/test_no_lemma_naming.py` (`_ALLOWED_LEMMA_SYMBOLS`, `_LEMMA_OWNING_FILES`, `_LEMMA_OWNING_COLUMNS`, `_migration_modules`, `_reflected_column_names`/`_reflected_columns_with_table`, leg de OpenAPI servido — ahora incluye las rutas y DTOs de anotación); `apps/web/tests/contracts/no-lemma-naming.test.ts` (`ALLOWED_LEMMA_SYMBOLS`, `LEMMA_OWNING_FILES`); `apps/api/tests/unit/test_annotation_contract.py::test_the_pinned_json_schema_names_no_lemma_or_lexeme_outside_the_allow_list` | 1.6–1.8 (Phase 1); 5.1–5.7 (Phase 5, leg de OpenAPI + UI), `lemmatization-pos`; C1 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido — corte 1 cierra el mecanismo (lista de admisión de nombres exactos por igualdad estricta); corte 5 lo ejercita de extremo a extremo por primera vez a través de HTTP: `api/dtos/annotation.py` fija explícitamente `Field(title=...)` en minúscula para `lemma`/`lemma_origin`/`automatic_lemma`/`lemma_confidence` — Pydantic auto-titula cada campo (`"Lemma"`, con mayúscula) y ese título se publica en `components.schemas.*` del OpenAPI servido, donde el guard NO exime docstrings; sin el `title=` explícito, el leg de OpenAPI fallaba con 4 coincidencias reales, cerrando así la brecha de no-vacuidad que antes solo probaba una tabla-sonda sintética. En el frontend, ningún encabezado en inglés («Lemma») sobrevive el guard exacto (`"Lemma" ≠ "lemma"`); `AnnotationTable.tsx` usa copy en español («Lema», singular, un solo `m`) exactamente como anticipa design §P6 — «Lema» no coincide con `lemma\|lemas\|lexeme\|lexema` (que exige doble `m`), así que ninguna entrada nueva en la lista de admisión fue necesaria. Un renombrado a un nombre con forma de lema fuera de la lista sigue fallando (AC-003-24 escenario 2). **C1**: antes de la corrección, la exención por igualdad exacta contra `_ALLOWED_LEMMA_SYMBOLS`/`ALLOWED_LEMMA_SYMBOLS` no tenía ningún vínculo con el sitio de declaración, el módulo propietario ni la capacidad — renombrar `Occurrence.normalized_text`, `FormFrequencyResponse.display_form` o la propiedad `normalized_form` del schema `import.v1.json` (pinneado) al nombre exacto `lemma` producía **cero violaciones en todos los legs**, incluido el de TypeScript. Ahora cada símbolo de la lista de admisión está vinculado a los archivos/tablas/segmento de esquema OpenAPI que legítimamente lo declaran (`_LEMMA_OWNING_FILES`, `_LEMMA_OWNING_COLUMNS`, `_OPENAPI_LEMMA_OWNING_SCHEMA` en el backend; `LEMMA_OWNING_FILES` en el frontend) — la exención exige AMBAS condiciones (coincidencia exacta Y archivo/tabla/segmento propietario), nunca el símbolo por sí solo. Reproducido en RED antes de la corrección con los tres escenarios exactos citados arriba (backend Python leg, JSON leg de `import.v1.json`, leg de columnas reflejadas con una tabla-sonda) y con el equivalente TypeScript (`normalized_form -> lemma` en `src/types/imports.ts`); todos ahora fallan como se espera. **Corrección (R2, `judgment-day` ronda 2):** `apps/api/tests/unit/test_annotation_contract.py::test_the_pinned_json_schema_names_no_lemma_or_lexeme_outside_the_allow_list` — citada arriba como evidencia de C1 — en realidad NO fue tocada por la corte C1: seguía usando el predicado sin vincular anterior a C1 (`text not in _ALLOWED_LEMMA_SYMBOLS`, sin ninguna comprobación de EN QUÉ RUTA ocurre la coincidencia), así que producía cero violaciones para exactamente el mismo tipo de renombrado que C1 cerró en el resto de los legs. Ahora usa el mismo predicado vinculado por segmento exacto que `test_no_lemma_naming.py::_json_violations` (`_OWNING_PATH_SEGMENT = "occurrence"` más el mismo `_path_segments`, duplicado localmente en este archivo porque ya duplicaba `_json_strings` por el mismo motivo de independencia entre módulos de prueba). Reproducido en RED con `test_renaming_a_non_owning_property_to_lemma_still_fails_outside_the_allow_list`, revirtiendo temporalmente al predicado sin vincular y confirmando cero violaciones para el mismo renombrado `provenance.source -> lemma` que ahora sí se detecta |
| REQ-003-022 | `PROPN` se persiste como cualquier otra etiqueta; ningún filtro de nombres propios se envía en esta capability | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-23 | `apps/api/tests/unit/test_annotation_domain.py` (ausencia de cualquier lógica de filtrado); `apps/web/tests/components/AnnotationTable.test.tsx::test_shows_propn_unfiltered_exactly_like_any_other_tag` | 2.1–2.2 (Phase 2); 5.6–5.7 (Phase 5, leg de frontend), `lemmatization-pos` | Cumplido — corte 5 cierra el escenario positivo en el leg de frontend: una aparición con `pos: "PROPN"` se renderiza sin filtro, exclusión ni caso especial en `AnnotationTable.tsx` (el mapa de etiquetas la localiza igual que cualquier otra). El escenario positivo de backend con el adaptador real (`PROPN` persistido end-to-end) quedó cerrado en el corte 4 |
| REQ-003-007 | Cada anotación automática registra procedencia recuperable (7 campos de §2.4); los cinco primeros son no nulos; `language` se registra por ejecución, nunca por defecto | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-07 | `apps/api/tests/unit/test_annotation_models.py::test_annotation_provenance_maps_all_seven_fields`, `::test_annotation_provenance_occurrence_id_is_unique`; `apps/api/tests/integration/test_annotation_write_repository.py::test_write_sets_pos_and_lemma_and_inserts_provenance`, `::test_a_re_run_replaces_the_previous_provenance_row`; `apps/api/tests/integration/test_annotation_read_repository.py::test_read_exposes_provenance_and_confidence_fields`; `apps/api/tests/integration/test_spacy_analyzer.py::test_construction_runs_the_self_check_and_succeeds_with_the_real_model` | 3.1–3.4, 3.7–3.10 (Phase 3); 4.1, 4.10 (Phase 4), `lemmatization-pos` | Cumplido — corte 4 cierra la última pieza pendiente: `SpacyLinguisticAnalyzer.identity` reporta `source="spacy"`, `model_name` (el nombre del paquete instalado) y `model_version` (`nlp.meta["version"]`, la versión real del pipeline instalado) — ya no una `AnalyzerIdentity` construida a mano en el test. `AnnotateImport` pasa esa identidad y el `language` explícitamente solicitado a `writer.write()` sin inferencia alguna |
| REQ-003-011 | El camino de escritura escribe automáticos y procedencia incondicionalmente, sin leer `ManualCorrection`, y no inserta/actualiza/borra ninguna fila de esa tabla; la tabla se envía solo con esquema (R6) | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-11 | `apps/api/tests/unit/test_annotation_write_repository_isolation.py` (guard AST completo; extendido en la remediación `judgment-day` C4 con substring en literales y plegado de concatenación `+`); `apps/api/tests/integration/test_annotation_write_repository.py::test_write_ignores_an_existing_manual_correction_and_writes_unconditionally`; `apps/api/tests/integration/test_annotation_read_repository.py::test_reprocessing_leaves_the_correction_byte_identical`, `::test_property_a_seeded_correction_survives_any_generated_reannotation` | 3.6–3.10 (Phase 3, `lemmatization-pos`); C4 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido — R2/R3/R6 quedan cerrados a nivel de repositorio, la capa donde esta capability los puede probar: `annotation_write_repository.py` no importa, referencia ni menciona `ManualCorrection` en ninguna posición del AST (import, nombre, atributo, literal de cadena) — verificado con un mutation-check real (se añadió el import, se observó el `AssertionError`, se revirtió). El escenario 3 de AC-003-11 («inspeccionado estructuralmente, sin referencia a la tabla de corrección») está probado literalmente, no solo por inferencia. Verificado además de extremo a extremo contra el repositorio de lectura: una corrección sembrada sobrevive byte-idéntica a una reanotación con valores nuevos, tanto en un caso fijo como (Hypothesis) para cualquier etiqueta UPOS y confianza generadas. **Corrección (verify-report WARNING-8):** esta fila conservaba la nota «Pendiente para el corte 4: que la reanotación real la dispare `AnnotateImport`…» pese a estar marcada `Cumplido`. El corte 4 cerró exactamente eso: `test_annotate_import_properties.py::test_property_two_runs_produce_identical_pos_and_lemma_only_processed_at_differs` dispara la reanotación a través de `AnnotateImport.execute()`, no de una llamada directa al repositorio. **C4**: el detector solo comparaba por igualdad exacta contra el literal completo `"ManualCorrection"`/`"manual_correction"` — un SQL crudo que INCRUSTA el nombre como subcadena (`text("DELETE FROM manual_correction ...")`) o una concatenación de literales partidos (`"manual" + "_correction"`, `"Manual" + "Correction"`) pasaban sin detección, confirmado en RED antes de la corrección. Ahora el chequeo de literales de cadena busca subcadena en vez de igualdad exacta, y un plegado de constantes recorre cualquier cadena de `BinOp` con `+` (a cualquier profundidad) para detectar el resultado concatenado. Esto exigió añadir una exención de docstring (idéntica a `test_no_lemma_naming.py::_docstring_constant_ids`) porque el propio docstring de MÓDULO de `annotation_write_repository.py` menciona «ManualCorrection» en prosa legítima (verificado: es la única constante de cadena de ese archivo que contiene el nombre prohibido), y el chequeo por subcadena lo habría marcado como violación de sí mismo. **Corrección (R2, `judgment-day` ronda 2):** esa primera exención cubría CUALQUIER docstring — módulo, clase Y función — lo cual abrió una vía de evasión propia: un docstring es una cadena ordinaria alcanzable en tiempo de ejecución (`fn.__doc__`), así que una función cuyo docstring FUERA el SQL crudo (ejecutado, p. ej., vía `text(fn.__doc__)`) llegaba a `manual_correction` con cero violaciones reportadas. `_DOCSTRING_OWNERS` ahora se limita a `(ast.Module,)` — la única exención genuina necesaria, ya que el único docstring del módulo guardado que menciona el nombre prohibido es el de módulo, no uno de clase o función. Reproducido en RED con dos pruebas nuevas (`test_a_function_docstring_holding_the_raw_forbidden_sql_would_be_caught`, `test_a_class_docstring_holding_the_raw_forbidden_sql_would_also_be_caught`) y un caso de control que prueba que el mismo SQL fuera de un docstring ya se detectaba. Además, el docstring de `annotation_write_repository.py` y `design.md` §P3 afirmaban que el aislamiento era «structurally provable» (prueba estructural) — una afirmación más fuerte de lo que el guard demuestra: `_folded_string` solo reconoce cadenas de `+`; una f-string, `str.join` o formateo con `%` que ensamblara `"manual_correction"` en tiempo de ejecución evadiría el detector hoy. Ambos docstrings se corrigieron para describir la garantía real (acotada a los patrones de construcción de cadena que el guard reconoce), en vez de cerrar exhaustivamente cada posible ruta de construcción de cadena, decisión explícita del alcance de la ronda 2 |
| REQ-003-014 | Una ejecución de anotación es atómica por import: un fallo en cualquier punto no debe dejar ninguna aparición parcialmente anotada, ninguna procedencia huérfana, ni versiones de modelo mezcladas | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-15; spec hook H10 (invariante de no-mutación) | `apps/api/tests/integration/test_annotation_write_repository.py::test_a_failure_mid_transaction_leaves_zero_rows_touched`, `::test_write_with_an_empty_sequence_is_a_no_op`; `apps/api/tests/unit/test_annotate_import.py` (los 6 modos de fallo, cada uno con `assert writer.calls == []`); `apps/api/tests/integration/test_annotate_import_properties.py::test_property_annotation_never_mutates_raw_text_normalized_text_or_position` (corte de remediación) | 3.6–3.8 (Phase 3); 4.9–4.10 (Phase 4), `lemmatization-pos`; corte de remediación (`feat/spec-003-06-remediation`) | Cumplido — corte 4 cierra la primera línea de defensa que corte 3 dejó pendiente: `AnnotateImport._validate_and_assemble` valida TODO (longitud/orden, pertenencia a `UPOS_TAGS`, rango de confianza) fuera de cualquier transacción, antes de que `writer.write()` se invoque siquiera una vez — probado con los 6 modos de fallo del stub (retorno corto, `NN`, `1.4`, choque a mitad de análisis, fallo en el segundo token, idioma no soportado), cada uno afirmando `writer.calls == []`. Junto con la segunda línea de defensa (transaccional, corte 3), la atomicidad queda cerrada por ordenamiento y por transacción, design §P5. **Corrección (verify-report WARNING-8):** esta fila conservaba una nota de discrepancia sobre la lista «Closes» de la fase 4 en `tasks.md`, sin valor a día de hoy — se sustituye por la referencia a la property Hypothesis del corte de remediación que cierra AC-003-14 escenario 2 / hook H10 (ver también REQ-003-002 y CRITICAL-4 del `verify-report.md`) |
| REQ-003-015 | Exactamente una revisión Alembic aditiva añade `lemma`, la procedencia y `ManualCorrection`; no debe alterar ninguna columna que `002-text-import` creó; `downgrade()` debe devolver el esquema a la base SPEC-002 | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-16 | `apps/api/tests/integration/test_alembic_0003.py` | 3.1–3.2 (Phase 3, `lemmatization-pos`) | Cumplido — `migrations/versions/0003_annotation.py` (`down_revision = "0002_book_occurrence"`) añade `occurrence.lemma`, `annotation_provenance` y `manual_correction` usando `batch_alter_table` tanto para el `ADD COLUMN` como para el `DROP COLUMN` (SQLite < 3.35 no soporta `DROP COLUMN` directo). Verificado: `upgrade` seguido de `downgrade -1` deja `book`/`occurrence` estructuralmente idénticos a la base `0002_book_occurrence` (mismos nombres de columna, mismo tipo, misma nulabilidad); una fila de `occurrence` sembrada antes de la actualización sobrevive con `raw_text`/`normalized_text`/`position` sin cambios y `lemma` en `NULL`. `test_alembic_0002.py` se re-fijó a nombres de revisión explícitos (antes usaba `head`/`-1`, que ahora aterrizan en `0003`, no en el baseline `0001`) |
| REQ-003-004 | El analizador devuelve exactamente una anotación por token de entrada, en el mismo orden; un desajuste de longitud, contenido o índice falla con `ANNOTATION_FAILED` sin reconciliación | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-04 | `apps/api/tests/unit/test_annotate_import.py::test_a_short_return_fails_annotation_failed_and_writes_nothing`, `::test_a_reordered_same_length_output_fails_annotation_failed_and_writes_nothing`, `::test_same_text_swap_without_reassigning_source_index_fails_and_writes_nothing`, `::test_execute_writes_one_validated_record_per_token_in_order`; `apps/api/tests/integration/test_annotate_import_properties.py::test_property_reversing_the_reader_row_order_does_not_change_the_mapping`, `::test_property_a_scrambled_analyzer_output_fails_annotation_failed_and_writes_nothing` | 4.9–4.10 (Phase 4, `lemmatization-pos`); C6 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido — `AnnotateImport._validate_and_assemble` compara `len(annotations)` contra `len(tokens)` antes de cualquier otra cosa y falla con `AnnotationFailedError` ante cualquier desajuste de longitud. El emparejamiento `occurrence_id`↔anotación exige igualdad de contenido entre `LinguisticAnnotation.raw_text` y `token.raw_text`, además de `source_index == position`; no depende de identidad de objetos. La regresión de intercambio con el mismo texto y sin reasignar el índice demuestra el caso cubierto. La property `test_property_a_scrambled_analyzer_output_fails_annotation_failed_and_writes_nothing` conserva la forma enviada: una salida del analizador desordenada falla y no escribe nada. |
| REQ-003-009 | La API expone `pos_confidence` y `lemma_confidence` por aparición, incluido `NULL` explícito; la UI distingue ausencia y valor numérico | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-09 | `apps/api/tests/integration/test_spacy_analyzer.py`, `apps/api/tests/unit/test_annotate_import.py`, `apps/api/tests/api/test_annotation_route.py`, `apps/api/tests/unit/test_annotation_contract.py`, `apps/web/tests/components/AnnotationTable.test.tsx`; `apps/api/tests/integration/test_attribute_ruler_enumeration.py::test_runtime_enumeration_has_only_computed_shape_and_partition_predicates` | Phase 4–5, `lemmatization-pos`; 4.1–4.4 (`08d-1-attribute-ruler-enumeration`) | Cumplido — la enumeración citada conserva los detalles específicos del modelo fuera de la prosa gobernada |
| REQ-003-013 | Un import persistido antes de esta capability es anotable usando solo sus datos almacenados; sin re-subida, sin texto fuente guardado, sin re-tokenización | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-14 | `apps/api/tests/unit/test_annotate_import.py::test_execute_writes_one_validated_record_per_token_in_order`, `::test_execute_of_a_book_with_zero_occurrences_writes_an_empty_sequence`; `apps/api/tests/integration/test_annotate_import_properties.py` (usa siempre `Occurrence.raw_text` ya persistido, nunca texto fuente) | 4.9–4.10 (Phase 4, `lemmatization-pos`) | Cumplido — `AnnotateImport.execute()` lee exclusivamente `AnnotationReader.read(book_id)` (`Occurrence.raw_text` ordenado por `position`, vía el mismo repositorio de lectura de la corte 3) y nunca recibe ni requiere el texto original del archivo; el mismo camino de código sirve tanto para un import recién creado como para uno preexistente — no hay rama especial para «ya anotado antes» |
| REQ-003-016 | La anotación se ejecuta local y sin conexión; ningún token, forma textual, lema o forma normalizada se transmite a ningún endpoint de red | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-17 | `apps/api/tests/integration/test_spacy_analyzer.py::test_construction_and_analysis_succeed_with_outbound_network_disabled` | 4.3 (Phase 4, `lemmatization-pos`) | Cumplido — con `socket.socket.connect`/`connect_ex` parcheados para lanzar excepción ante cualquier intento de conexión saliente, tanto la carga del pipeline (`spacy.load`, que solo lee `site-packages` instalado) como el análisis completan con éxito — cero conexiones intentadas en todo el ciclo de vida del adaptador |
| REQ-003-019 | Ningún registro de log ni cuerpo de error de anotación contiene texto importado, forma textual, forma normalizada, lema ni valor corregido; los fallos se identifican por código, `id` de import y, cuando aplica, posición del token — nunca su texto | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-20 | `apps/api/tests/unit/test_annotate_import_logging.py` (leg completo: éxito sin fuga, los 5 códigos de fallo con código+id+posición cuando aplica, cero trazas de pila); `apps/api/tests/unit/test_annotate_import.py::test_a_missing_model_os_error_becomes_analyzer_unavailable_and_writes_nothing`, `::test_a_malformed_pipeline_key_error_becomes_analyzer_unavailable` | 4.13 (Phase 4, `lemmatization-pos`); C5 (`judgment-day` remediation, `feat/spec-003-07-judgment-fixes`) | Cumplido (leg de backend interno — el cuerpo de error HTTP concreto llega con la ruta de la corte 5) — `AnnotateImport._log_failure` emite exactamente `code=<código> import_id=<id> position=<posición o "-">` vía `logger.warning` (nunca `logger.exception`, que renderizaría una traza); verificado con el sentinel `zzqxsentinel` inyectado en el texto de un token — cero registros capturados lo contienen tanto en la ejecución exitosa como en cada uno de los 5 caminos de fallo alcanzables en esta capa (`IMPORT_NOT_FOUND`, `UNSUPPORTED_LANGUAGE`, `ANALYZER_UNAVAILABLE`, `ANNOTATION_FAILED` con posición, `ANNOTATION_FAILED` sin posición atribuible). Mutation-check real: se añadió temporalmente un log de los tokens crudos, se confirmó el fallo del test, se revirtió. **C5**: antes de la corrección, un `OSError`/`KeyError` crudo de `registry.resolve()` (modelo faltante o pipeline mal formado) escapaba de `execute()` sin traducir — sin registro de `_log_failure` y, si el mensaje de la excepción real lo llevaba (como el `E050` real de spaCy), con el nombre del modelo o una ruta de filesystem filtrándose en el cuerpo del error. Ahora se traduce a `AnalyzerUnavailableError` (503) y se registra igual que cualquier otro fallo, sin exponer el mensaje crudo. Además, `UnsupportedLanguageError` levantado por el propio `analyze()` del adaptador (contrato documentado en `ports.py`, ADR-0008) ya no es capturado por el `except Exception` genérico y degradado a `ANNOTATION_FAILED` (500) — se re-propaga como el 422 real que es |
| REQ-003-020 | La anotación es determinista y write-idempotente: una secuencia de tokens fija con un modelo fijo produce el mismo `pos`/`lemma` en cada ejecución; solo `processed_at` puede avanzar. La idempotencia ingenua «lematizar un lema devuelve ese lema» NO se afirma (§5 AMB-1) | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-21 | `apps/api/tests/integration/test_annotate_import_properties.py::test_property_two_runs_produce_identical_pos_and_lemma_only_processed_at_differs` | 4.11 (Phase 4, `lemmatization-pos`) | Cumplido — property Hypothesis: para secuencias de tokens generadas, dos ejecuciones de `AnnotateImport.execute()` sobre el mismo import con un analizador fijo producen `pos`/`lemma` idénticos en cada posición; solo `processed_at` avanza. El módulo documenta explícitamente, en su docstring, por qué la forma ingenua NO se afirma: un lema analizado sin contexto de oración puede reclasificarse (`saw` → `VERB`/`see` en vez de `NOUN`/`saw`), que sería un defecto correcto del modelo reportado como fallo del test |
| REQ-003-021 | La independencia de orden se limita al orden de procesamiento (tamaño de lote, orden de lectura/escritura, orden entre imports), nunca al orden de los tokens — permutar los tokens de un documento cambia legítimamente `pos`/`lemma` (§5 AMB-3) | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-22 | `apps/api/tests/integration/test_annotate_import_properties.py::test_property_reversing_the_reader_row_order_does_not_change_the_mapping`, `::test_property_splitting_the_write_into_two_batches_does_not_change_the_mapping`, `::test_property_annotating_two_imports_in_either_order_does_not_cross_contaminate` | 4.12 (Phase 4, `lemmatization-pos`) | Cumplido — tres properties Hypothesis independientes cubren los tres ejes del requisito: orden de lectura invertido, tamaño de lote de escritura (una llamada a `write()` vs dos), y orden entre dos imports distintos — el mapeo `position → (pos, lemma)` de cada import es idéntico en cada caso. El módulo documenta explícitamente, en su docstring, que la invariancia de permutación de tokens NO se afirma, citando ADR-0006; el diseño ya impone que el analizador reciba el import completo como un único `Doc` (REQ-003-021, trampa de diseño §P5), así que ninguna implementación de esta corte podría fragmentar esa llamada aunque quisiera |
| REQ-003-012 | `POST /api/v1/imports` no ejecuta anotación; sus códigos de estado, cuerpo, errores y filas persistidas son idénticos a la base de `002-text-import`; toda aparición escrita por el camino de importación conserva `pos IS NULL`/`lemma IS NULL` | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-12 | `apps/api/tests/unit/test_import_contract.py::test_the_schema_stays_byte_identical_to_the_pre_annotation_baseline`; re-ejecución completa de la suite `002-text-import` (las 424 pruebas preexistentes de `002-text-import` pasan sin cambio; el recuento absoluto de la suite backend no se fija en esta fila porque crece con cada corte); `apps/api/tests/api/test_annotation_route.py::test_get_annotation_before_any_run_returns_null_provenance_and_null_fields` | 5.1 (Phase 5, `lemmatization-pos`) | Cumplido — `api/routes/imports.py` no fue modificado por esta corte; `import.v1.json` está fijado por SHA-256 (`def94cb6…554258`, 1852 bytes) frente a la base previa a la corte 5, y la suite completa de `002-text-import` pasa sin cambios. Una importación nunca anotada expone `pos`/`lemma` efectivos `NULL` con origen `automatic` a través de la nueva ruta `GET`, nunca un error — REQ-003-012 se cierra por la ausencia de cualquier código que conecte los dos caminos, no por una comprobación en tiempo de ejecución. **Corrección (verify-report WARNING-8, y su reincidencia):** esta fila citó primero «449 pruebas backend en total» y luego «452». Ambos quedaron obsoletos: el recuento real tras la remediación es `470`. Un recuento absoluto mantenido a mano deriva en cada corte, así que se eliminó y se conserva solo el invariante que esta fila necesita demostrar — que las 424 pruebas de `002-text-import` siguen pasando sin cambio. No vuelvas a fijar un total aquí |
| REQ-003-017 | La anotación tiene su propio contrato, distinto de `import.v1.json` v1, que permanece byte-idéntico y sin ganar propiedades | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-18 | `apps/api/tests/unit/test_annotation_contract.py` (schema Draft 2020-12, forma de ocurrencia, envoltorio de error, DTOs); `apps/api/tests/unit/test_import_contract.py::test_the_schema_stays_byte_identical_to_the_pre_annotation_baseline` | 5.1–5.3 (Phase 5, `lemmatization-pos`) | Cumplido — `api/schemas/annotation.v1.json` (`$id: urn:wheel-vocabulary:annotation:v1`) es un fichero nuevo y separado, agrupado por aparición (no por forma normalizada), con `provenance` izada al sobre y ambas claves de confianza siempre presentes, `null` incluido (C5). `import.v1.json` no fue tocado por esta corte (ver fila REQ-003-012) |
| REQ-003-018 | El frontend no duplica ninguna regla lingüística: renderiza exactamente lo que devuelve la API; la localización presentacional de `pos` es total sobre las 17 etiquetas y una etiqueta no mapeada degrada a la etiqueta cruda, nunca a una celda vacía; la vista es navegable por teclado y no depende solo del color | `openspec/specs/003-lemmatization-pos/spec.md` — AC-003-19 | `apps/web/tests/components/AnnotationTable.test.tsx` (verbatim, `PROPN` sin filtrar, etiqueta no mapeada degrada, confianza `null` vs. numérica distinguible como texto); `apps/web/tests/contracts/no-linguistic-rules.test.ts` (manifiesto `FRONTEND_FEATURE_MODULES` extendido con los tres ficheros de anotación; nuevo patrón de identificador para `lemmatiz`/`tokeniz`/`resolveEffective`/`correctionPrecedence`) | 5.6–5.8 (Phase 5, `lemmatization-pos`) | Cumplido — `AnnotationTable.tsx` no contiene lógica de lematización, etiquetado, normalización ni resolución de precedencia; el mapa de etiquetas UPOS→etiqueta legible es total y degrada al valor recibido para cualquier entrada no cubierta (probado con la etiqueta sintética `ZZQX`); ninguna celda queda vacía; el origen (`pos_origin`/`lemma_origin`) se renderiza verbatim, nunca traducido. Tabla HTML semántica sin controles interactivos propios — navegable por teclado por construcción; la distinción `null`/numérico de confianza es puramente textual ("No informada" vs. el número recibido), nunca solo por color |


---

## Update rules

### Reglas de actualización

1. **Añadir fila** cuando se introduce un nuevo `REQ-<feature>-<n>` en cualquier
   spec o change. La fila debe incluir todos los campos; Status inicial = `Pendiente`.

2. **Actualizar Estado** cuando la aceptación del requisito se demuestra:
   cambia de `Pendiente` / `En progreso` a `Cumplido`. El agente o desarrollador
   que cierra la tarea referenciada es responsable de la actualización.

3. **Nunca eliminar filas.** Cuando un requisito queda obsoleto o es
   reemplazado, marcar `Bloqueado` o añadir una nota en la columna Statement
   con la referencia al requisito sucesor. El estado histórico se preserva.

4. **`Bloqueado` requiere razón:** la columna Statement (o una nota en la misma
   fila) debe indicar qué bloquea el requisito y quién debe desbloquearlo.

5. **Responsabilidad de fila:** la persona o agente que cierra la tarea
   referenciada en Task(s) actualiza el Estado en esta matriz. Los ficheros
   `traceability.md` por spec siguen siendo la fuente de verdad para las filas
   de su feature; esta matriz agrega filas de alcance cruzado o landmark.

---

## Notes

Additional rows will be added as new REQ-* IDs land. The four amendment REQs
(Family F, Slice E) will appear as `Pendiente` here until Slice E lands, then
flip to `Cumplido`. The Slice A + C divergences flagged for verify are
separately tracked in Engram observation #2271.

La familia `REQ-TESTHYG-*` cubre higiene de la suite de pruebas (fugas de
recursos y avisos de obsolescencia de dependencias). Deliberadamente no usa el
rango `REQ-001-*`: `apps/api/tests/unit/test_traceability.py` exige exactamente
una fila por cada `REQ-001-001`…`REQ-001-018`, así que ampliar ese rango
rompería esa comprobación.

**Recuento de escenarios de `lemmatization-pos` (corte de remediación,
`feat/spec-003-06-remediation`).** El `verify-report.md` de este cambio
contó `64` encabezados `#### Scenario:` (`58` en
`specs/003-lemmatization-pos/spec.md` + `6` en el delta de
`specs/002-text-import/spec.md`) frente a los `61` que citaba el «launch
brief» original de la fase de verificación. Recontado de forma independiente
en este corte con `grep -c "^#### Scenario:"` sobre ambos ficheros: el
resultado es, de nuevo, `58 + 6 = 64`, coincidiendo con el `verify-report`.
**Ningún artefacto versionado del repositorio afirma «61»** — ese número
solo existió en el mensaje de lanzamiento efímero de la fase de
verificación (no un fichero); `spec.md` no declara ningún campo de recuento
de escenarios en su tabla de metadatos (§1) que pudiera contradecir el
recuento real de encabezados. Conclusión: no hay ningún documento del
repositorio que corregir — el recuento correcto es `64`, y es el que ya usa
`verify-report.md`.
